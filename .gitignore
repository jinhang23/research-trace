# 含 space 与 token，绝不入库
config.json

# 编译产物是文件系统的纯函数，随时可以删掉重建
dist/

# 写入用的跨进程锁（projects/<项目>/.trace-lock）。里面一个字节都没有，
# 删掉不丢任何信息，下次写入自己重建——是运行期状态，不是记录。
# 数据仓如果是另一个 git 仓库，那边也要加同一行。
.trace-lock

# 跨 session 的私人工作笔记，留在本地
claude_logs/

__pycache__/
*.pyc
.pytest_cache/
.venv/

# 打包产物
*.egg-info/
build/
