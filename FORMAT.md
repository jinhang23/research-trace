# 科研步骤记录格式标准 v1

这份文档规定仓库里那几种 markdown 怎么写：

- 每一步的 `note.md`（第 2–10 节，外加第 14 节的 `moved:`、第 15 节的 `branch:` / `decision:`、
  第 16 节的 `pipeline:` 和第 17 节的 `chapter:`）
- 每个项目的 `project.md`——项目级洞察、删除记录和 `result:`（第 11 节、第 16 节）
- 它们的翻译 `note.<lang>.md` / `project.<lang>.md`（第 13 节）

人和 agent 都按它写，也都按它读。

它不是风格建议，是**契约**：`trace_cli.py check` 会按它给出警告，
可溯源性评级（L0–L4）也按它判定，网页详情面板按它渲染。

第 12 节列出渲染器**实际**支持和有意不支持的 markdown 语法——按标准写之前先看一眼那一节，
免得写了一段渲染器读不出来的语法。

**要写翻译就先读第 13 节。**那一节是翻译文件唯一的规范来源，最要紧的一条是：
翻译文件的 front-matter 里**只准有 `title`**，结构字段一个都不许重复。

最容易被混为一谈的一对概念是 `parent` 和 `input`：**`parent` 是「我当时接着哪一步想」，
`input` 是「这些字节从哪来」**。第 6 节整节在讲这件事，动手记数据依赖之前先读它。

**分了叉就再读一遍第 15 节。**同一个问题的两个答案（只能选一条走下去）和「接着往下做」
在树上长得一模一样，靠 `branch:` 分开；判别只要问一句「这两条能同时成立吗」。

**要写论文、要把方法给别人照着做，读第 16 节。**同一个项目有**两条路径**：
**开发路径**（这棵树的全部，含走不通的和还没决定的岔路口）和**定稿流程**
（真正产出成果的那一条链）。前者是**记录**，后者是**方法**，两个都要留。
定稿流程整条都是算出来的，全项目只有 `project.md` 里那一行 `result:` 要人写。

**一个项目内部还要分主实验 / 消融实验 / 数据准备，读第 17 节。**一行 `chapter:` 写在
开启那条线的**那一步**上，整条子树沿 parent 继承。判别同样只要一句话：**这几块要不要
都留着、都写进论文？**要 → 是几个**章节**；只能留一个 → 那是**分叉**（第 15 节）。

---

## 0. 一条总原则

> **可视化必须从已经可读的文本里长出来，不能反过来。**

一张 PNG 里的曲线，人看得见，LLM 看不见——信息在读者之间是不对等的。
一个 markdown 表格，人看到对齐的列，LLM 读到同样的数字——**信息完全对等**，
渲染只是加成。

所以格式的优先级是：

| 优先 | 形式 | 人 | LLM | 说明 |
|---|---|---|---|---|
| 1 | markdown 表格 | 对齐的列 + 数值底纹 | 同样的数字 | 首选。指标对比一律用它 |
| 2 | front-matter 字段 | 元信息栏 | 结构化字段 | commit / path / repro 这类 |
| 3 | 围栏代码块 | 带语言标记，可复制 | 原文 | 命令、配置、日志片段 |
| 4 | 带图注的图片 | 看得见图 | **只看得见图注** | 图注必须承载结论 |
| 5 | 裸图片 | 看得见图 | 什么也没有 | **不允许**，check 会报警告 |

第 4 和第 5 的差别就是一句话的事，而那句话是图对一半读者的全部信息。

---

## 1. 参照了谁

这套格式不是凭空设计的，几条关键决定各有出处：

| 借鉴 | 借了什么 |
|---|---|
| **ADR**（Architecture Decision Records, Nygard 2011） | Context / Decision / Consequences 这个三段式。我们的「为什么 / 做了什么 / 结论」是同一个骨架——先说处境，再说做了什么决定，最后说后果 |
| **W3C PROV** | `wasDerivedFrom` 这条关系。我们的 `parent` 就是它的最小可用形式；PROV 允许多父，我们刻意收窄成单父森林（见 README） |
| **Model Cards**（Mitchell et al. 2019）、**Datasheets for Datasets**（Gebru et al. 2018） | 「必填小节」这个思路：与其让人自由发挥，不如规定几个必须回答的问题，空着就是缺陷 |
| **Conventional Commits** | 元信息前置、机器可解析。我们的 front-matter 是同一个作用 |
| **CommonMark** | 正文的基线语法。不发明新标记 |
| **FAIR 原则** | 可定位（L2）这一级直接对应 Findable / Accessible——东西在哪必须记下来 |

---

## 2. 文件结构

```markdown
---
id: 007
parent: 005
status: done
title: 加入标题字段，准确率 0.943 → 0.951
date: 2026-03-11
commit: c1d2e3f
author: agent:claude
tags: features, transformer
moved: 2026-03-12 | 004 | 005 | human | 标题字段是在 005 的清洗结果上做的，挂 004 是记错了
input: 005 | agnews-clean/train.jsonl
path: /blue/<组>/<用户>/data/agnews-clean | input | 去重后的训练集 | n=120000 size=12884901888 checked=2026-03-11
path: /orange/<组>/<用户>/ckpt/run042/best.pt | output | 权重 | size=277872640 sha256=7d4e1a9c
code: snapshot | /orange/<组>/<用户>/run_snapshots/20260311 | manifest=MANIFEST.md5 n=43
repro: verified | 2026-08-08 | agent:claude | 干净 split 重跑 3 个种子，0.9506±0.0008
---

## 为什么
## 做了什么
## 结果
## 结论
## 下一步
```

front-matter 是**机器记录**，正文是**人的判断**。网页上前者用等宽体、后者用衬线体，
一眼分得清哪些是自动来的、哪些是自己想的。

用得上的键只有这十九个。别的键写进去不会报错也不会被用（解析器读得下来，构建时丢掉）；
**没有冒号的行**才会被忽略并给一条 `bad_front_matter_line` 警告：

| 键 | 说明 |
|---|---|
| `id` | 服务端分配。**写下就不再改、不重发**——笔记里的「见 002b」和论文脚注里的引用靠它永远有效 |
| `parent` | 单父，同项目内的已有 id。空 / `none` / `-` 都表示这是一棵树的根。**可以改，但每改一次要追加一条 `moved:`**，见第 14 节 |
| `branch` | **这一步和它 parent 之间那条边是什么意思**：`extends`（默认，不写就是它）/ `alternative`（我是一个互斥候选）。见第 15 节 |
| `status` | 只有 `wip` / `done` / `dead` 三个。认不出的值回退成 `wip` 并给警告 |
| `title` | 一行摘要。数字放正文的「结果」小节，别塞进标题 |
| `decision` | **这一步底下那个岔路口在决定什么**（写在分叉点身上，不是写在候选身上）。自由文本，见第 15 节 |
| `pipeline` | **这一步和定稿流程的关系**，用来推翻算出来的结论：`include`（闭包够不到它，但它确实是流程的一环）/ `exclude`（不算流程）。**必须写理由**，见第 16 节 |
| `chapter` | **这一步开启的章节**（主实验 / 消融实验 / …）：`<章节名> \| <这个章节是干什么的>`。只写在开启那条线的**那一步**上，整条子树沿 parent 继承——别给每一步各写一遍。说明可选，见第 17 节 |
| `lang` | 可选。这份正文**自己**是什么语言（`zh` / `en` / `ja` …）。不写就是「没声明」，系统不猜。见第 13 节 |
| `date` | `YYYY-MM-DD`。不写的话由**服务端**填当天日期（不是浏览器时区） |
| `commit` | 跑这一步时的代码版本。等价于一条 `code: git`，见第 7 节 |
| `author` | `human` / `agent:claude` 这类。谁记的 |
| `key` | 幂等键。带着它重发同一个新建请求返回既有步骤，不会造出重复的一步 |
| `tags` | 逗号分隔 |
| `moved` | parent 被改动过的审计，见第 14 节。**可以重复多行，顺序即历史** |
| `input` | 数据依赖：这一步吃进去的产物来自哪一步，见第 6 节。**可以重复多行** |
| `path` | 外部产物的位置，见第 7 节。**可以重复多行** |
| `code` | 代码在哪（git / 快照目录 / 容器镜像），见第 7 节。**可以重复多行** |
| `repro` | 复现记录，见第 9 节。**可以重复多行** |

**可以重复的键恰好是这五个：`path` `repro` `input` `code` `moved`**（代码里是
`trace_core.MULTI_KEYS`）。其余的键重复时，后写的覆盖先写的。

写回文件时键的顺序是固定的（`id` `parent` `branch` `status` `title` `decision` `pipeline`
`chapter` `lang` `date` `commit` `author` `key` `tags` `moved` `input` `path` `code` `repro`），所以同样的
数据永远产出同样的字节——手写时顺序随意，但过一次工具之后会被排成这个样子。

`branch` 紧贴着 `parent`、`decision` 紧贴着 `title`，是因为它们各自修饰的就是上面那一行：
「`parent: 011` / `branch: alternative`」连着读才是一句完整的话。
`pipeline` / `chapter` 挨着 `decision` 排在 `lang` 上面，是因为这几行都是**人对结构下的判断**
（「这个岔路口在问什么」「这一步算不算方法的一部分」「它属于哪一章的方法」），
从 `lang` 往下才是机器记录区。`chapter` 紧跟 `pipeline`，是因为这两行回答的是同一份对外
产出的两个问题——论文里主实验和消融本来就是两段 Methods。

冒号左边是键、右边**整行**是值——这不是 YAML，所以 `title: 试了 3:1 采样` 是合法的，
值里也不需要引号（首尾成对的引号会被剥掉）。

"整行"要当真：**没有行尾注释**。写 `status: done   # wip | done | dead`，
值就是 `done   # wip | done | dead` 这一整串，落到 `bad_status` 警告上退回 `wip`。
整行以 `#` 开头才算注释。

---

## 3. 五个小节写什么

### 标题怎么写才被认出来

**二级标题（`## `）要和这五个名字逐字一致**：`为什么` / `做了什么` / `结果` /
`结论` / `下一步`（英文版见第 13 节的对照表）。评级和 `check` 就是按这几个名字去
正文里找内容的，写成 `## 为什么（承接上一步）` 或 `## Why not` 就等于没写，
而且不会有任何警告。

**下面可以自由用三级及更深的标题。**一节的内容包含它下面所有**更深**的标题及其内容，
只有同级或更浅的标题才结束这一节——这是 markdown 的常识语义，也是这份文档的承诺：

```markdown
## 做了什么
### 1 · 统计口袋蛋白含量
用 `mmcif_parse.py` 统计每个口袋里的残基组成，蛋白残基占比 > 0 的判为混合口袋。

### 2 · 配对
按 RMscore 阈值 0.5 配对，得到 4554 条。
```

这份记录写得很全，`## 做了什么` 有内容。（以前不是这样：解析器见到任何标题就切一节，
于是 `## 做了什么` 在 `### 1 · …` 那一行被截断、内容为空，整步判成 L0。作者只能靠猜
发现问题，然后补一句废话引言把评级骗上去——**评级一旦逼着人写废话，它就开始撒谎了。**）

一个副作用是好的：`# 一级标题` 开头的笔记里，`## 为什么` 照样是它自己的一节，
不会被吞进那个一级标题。

同名标题出现两次时，后写的覆盖先写的。

### 为什么（必填）

> **这是整个系统里唯一无法自动生成的字段。**日志能自动存，commit 能自动记，
> 环境能自动 freeze，只有「我当时为什么决定试这个」必须人写。

要包含两件事：**承接了上一步的什么发现**，**想验证什么假设**。

```markdown
## 为什么
基线的 TF-IDF 完全丢掉词序和上下文。假设：预训练语言模型带来的上下文表征
能把这部分信息补回来，且提升幅度应当明显大于任何特征工程。
```

反例（这些都等于没写）：
- 「试试 DistilBERT」——没说为什么试
- 「提升准确率」——那是目标不是假设
- 「（承接上一步的什么发现，想验证什么假设）」——模板没删

### 做了什么（必填）

改了哪些文件、跑了什么命令。**命令要完整到别人不看原文件也能重跑**。

```markdown
## 做了什么
- 输入改为 `[CLS] 标题 [SEP] 正文 [SEP]`，其余超参与 [[002]] 一致
- 环境：`/blue/<组>/<用户>/envs/agnews`（conda，见 environment.yml）

​```bash
python train.py --model distilbert --fields title,body --seed 0 --epochs 3
​```
```

### 结果

数字、表、观察。**指标一律用表格**（见第 4 节）。

### 结论（`done` / `dead` 必填）

假设成立与否，以及**为什么**。不确定就写不确定。

`dead` 的结论要写清**放弃的理由**——死胡同是这套系统最有价值的部分，
半年后你会想知道为什么当时放弃了 X。

```markdown
## 结论
**假设不成立。**高亲和力子集最多改善 0.05，在种子间方差（±0.09）之内。
问题不是损失函数，是这段区间的样本量本身太少。放弃这条路。
```

### 下一步

派生出哪些分支。写了它，下一步建步骤时「为什么」就已经有了半句。

### 三条只提示、不降级的写法诊断

`check` 和网页会额外报三条 **`warn` 级**提示。它们**不进 L0–L4**，一条都不会让等级掉下来：

| `code` | 什么时候报 | 想说什么 |
|---|---|---|
| `section_without_prose` | 一个已知小节下面只有子标题，一个字散文都没有 | 现在这不再判 L0（那是 bug，已修），但你多半是漏写了引言 |
| `table_without_explanation` | 同一节里有表格，却没有任何说明文字 | 一句「这张表说明了什么」就够 |
| `code_without_explanation` | 同一节里有代码块，却没有任何说明文字 | 一句「这段命令在干什么」就够 |

判据是「**同一个已知小节内**有没有任何说明文字」——带图注的图片算说明文字。
所以第 4 节的指标表 + 第 5 节的带图注曲线那种写法（正是本文推荐的写法）零警告。

**为什么只到提示为止。**L0–L4 问的是「这个结果追不追得到」，这三条问的是
「读起来顺不顺」。把风格问题塞进等级，人就会为了分数写废话——这一轮改动的起因
恰恰就是有人被迫在小节下补一句引言来骗过评级。

图注那一条是**例外**，它仍然降级（见第 5 节）：表格和代码块本身 LLM 读得到，
缺的只是一句结论；而没有图注的图对文本读者是**零信息**。

---

## 4. 指标表的写法

**指标对比一律用 markdown 表格，不要用图。**

```markdown
| 模型 | 准确率 | 宏 F1 | 训练耗时 |
|---|---|---|---|
| TF-IDF + LR | 0.897 ± 0.004 | 0.896 ± 0.005 | 40 s |
| DistilBERT | **0.943 ± 0.004** | **0.942 ± 0.004** | 18 min |
```

规则：

- **主指标放第一个数值列**，行按可比性排列（基线在最上面）
- **不用手写 `---:`**：整列都是数字的列会自动右对齐 + 等宽数字，小数点自然成列。
  需要强制时才写 `:---` / `---:` / `:---:`
- 最好的一行用 `**加粗**` 标出来。加粗不影响「这一列是不是数值列」的判定
- 带单位的值（`40 s`）会让整列被当成文字列，这是对的——不要为了对齐去掉单位
- 有方差就写进去（`0.943 ± 0.004`），没有方差的单次结果说明不了差异
- 单元格里的字面竖线要写成 `\|`（CommonMark 的转义），否则它会被当成列分隔符
  把这一行劈开。网页上「从 Excel 粘贴自动转表格」产出的就是这个写法

渲染时数值列会带一条**底纹条**，长度按列内相对大小，让「哪个最好」一眼可见。
底纹是纯粹的加成——LLM 读到的还是原来那几个数字，去掉渲染也不丢任何信息。

**「整列都是数字」具体怎么判**（实测过，按这个写就一定能拿到右对齐和底纹条）：

| 写法 | 判定 | 底纹按哪个值 |
|---|---|---|
| `0.943` · `1,024` · `87%` · `-4.5E+2` · `−0.5` | 数值 | 它自己 |
| `0.943 ± 0.004`（`∓` `+/-` `+-` 同理，空格可有可无） | 数值 | **主值 0.943**，误差项不参与 |
| `**0.943 ± 0.004**` | 数值 | 同上 |
| `40 s` · `18 min` · `c1d2e3f` | 文字 | — |
| `—` · `–` · `-` · `/` · 空格 | **占位**，不参与整列判断 | 这一格不画 |

占位横线是特意做的：一张表里某个模型跑不出某个指标，写一个 `—` 不应该让整列退回文字列。

## 5. 图的写法

图必须**独占一段**并**带图注**：

```markdown
![](loss_curve.png "训练/验证 loss。第 12 轮之后验证集开始回升，说明再往后
就是纯过拟合——这也是把 epoch 定在 3 的依据。")
```

- 图注写的是**这张图说明了什么**，不是「这是一张 loss 曲线」
- 没有图注的图，`check` 会报 `figure_without_caption`，评级卡在 L0
- 图注可以折行（上面这个例子就是），只要整段之间不出现空行
- 截图在网页编辑器里直接 `Ctrl+V` 粘贴即可，光标会落在 `![|]` 里让你接着打图注

**附件名尽量不要带空格和括号。** 带了就必须用 CommonMark 的尖括号形式包住目标，
否则空格后面的部分会被当成图注的开头：

```markdown
![](<loss curve.png> "第 12 轮之后验证集回升")
```

渲染器认这种写法（`trace_attach` 也是这么拼的），但 `check` 的图注检查看不见它——
所以带空格的文件名一旦漏写图注，**不会有任何警告**。这是「文件名里别放空格」
最实在的理由。

**为什么这么严**：读这条记录的 agent 只看得见 `![](loss_curve.png "……")` 这一行。
图注是图对它唯一的信息来源，也是半年后你自己的唯一线索。

## 6. `parent` / `input` / `[[交叉引用]]`：三种关系，别混着用

一句话版本：

> **`parent` 是「我当时接着哪一步想」，`input` 是「这些字节从哪来」。**

森林是**单父树**（记录的派生关系），数据流是 **DAG**（谁吃了谁的产物）。
这两件事经常一致，但**不总是**——不一致的那些恰恰是最需要写清楚的。

### 一个真实的例子

一条 RNA-蛋白口袋流水线里，016「口袋-配体配对」的输入同时来自两步：

```markdown
---
id: 016
parent: 013b
status: done
title: 口袋-配体配对
date: 2026-08-09
input: 013 | pocket_composition.csv
input: 014 | rmscore_pairs.csv
path: /orange/<组>/pairs.csv | output | 配对结果 | n=4554
---

## 为什么
013b 给出了纯 RNA 口袋的判定阈值，接着要把口袋和配体配起来。

## 做了什么
读 013 的 `pocket_composition.csv` 和 014 的 `rmscore_pairs.csv`，按口袋 id 内连接。

## 结果
4554 条配对。

## 结论
阈值 0.5 下配对率 78%，够用。

## 下一步
拿这份配对做打分模型的训练集。
```

树上只能挂一个父节点，所以 `parent: 013b`（我是接着 013b 那个判定往下想的）；
而实际消费的字节来自 013 和 014，写成两条 `input:`。

以前这件事只能在正文里写一句「本步的输入其实来自 013 和 014」——人读得懂，
机器读不到，于是「谁用了 013 的产物」永远得靠 `grep` 加人脑。

### 格式

```
input: <步骤 id> | <消费的是哪份产物>
```

竖线右边是自由文本，**写清消费的是哪份产物**（文件名最有用）。可以重复多行。
同项目内的 id；跨项目的依赖请在正文里写 `[[引用]]` 并说明。

### 三条 `warn` 级检查（都不中断构建）

| `code` | 什么时候 |
|---|---|
| `dangling_input` | `input:` 指向一个不存在的步骤 |
| `self_input` | `input:` 指向自己 |
| `input_cycle` | 数据依赖成环（A 的输入来自 B、B 的输入又来自 A） |

写入侧**不**校验目标存在——建立顺序不定（先建消费者、再补生产者是常事），
和 `parent` 悬空的既有处理一视同仁：说出来，然后继续画图。那一行文本一个字不改。

### 它怎么影响评级

**可溯源性沿数据依赖上溯。**「这一步的输入来自哪一步」正是溯源在问的那件事：
016 的口袋组成来自 013，而 013 连产物在哪都没记，那么「016 这个数字是怎么来的」
就是追不到底的——哪怕树上 016 挂在写得很全的 013b 底下。

所以整链等级取的是 **parent ∪ inputs 这个依赖闭包**里最弱的一环，平局时
依赖赢过自己、parent 先于 input（见第 10 节）。这也解释了一个乍看奇怪的现象：
**整链等级可能比 `lineage` 里任何一环都低**——最弱的那一环在数据依赖上，不在树上。
详情面板会告诉你它是从哪条边找到的。

### 哪些 `input:` 边是「汇回」

一条支线的产物，后来又参与了另一条线上的某一步——这件事**不需要新字段**，它就是
一条 `input:`。判据也只看树的形状，机械可判（权威实现是 `trace_core.compute_merges`）：

> **两端在同一棵树里，且谁都不是谁的祖先。**

满足这两条就是汇回，并且能顺带算出两条线**分家的那个岔路口**（LCA）。不满足的：

- 生产者在消费者的**祖先链上** → 这条数据依赖和树边走的是同一条路，树已经画出来了
- 两端在**不同的树**里 → 从来没在同一条线上过，谈不上「汇回」

这两种老实算成**普通数据依赖**，不硬凑。所以 `input:` 那一行**该怎么写还是怎么写**——
「是不是汇回」是读的时候现算的，磁盘上一个字都不为它多写。怎么画在树上见第 15 节。

### 那 `[[003b]]` 还有什么用

正文里写 `[[003b]]` 仍然渲染成跳转链接，并在 003b 的页面显示「被这些步骤引用」。
它和上面两个的分工是：

| 写法 | 表达什么 | 机器读得到吗 |
|---|---|---|
| `parent:` | 我接着哪一步想的 | 树的边 |
| `input:` | 我吃了谁的产物 | 数据流的边 |
| `[[003b]]` | 「这里和 003b 有关」——引用、对比、佐证 | 只是反向链接，没有语义 |

综合了 A 线和 B 线的**结论**（而不是产物）用 `[[]]`；真的读了对方产出的文件，
用 `input:`。

---

## 7. 东西在哪：`path:` 与 `code:`

GB 级的东西不进仓库，只记它在哪。**跑完实验一定要记**——半年后想复现，
光有代码不够，还得知道那份数据在哪。

### 7.1 `path:` 的分段规则（向后兼容是硬要求）

一行一条，按 `|` 切开：

- **第 0 段永远是「位置」**；
- 之后每一段，逐段判定：
  * 整段**恰好**是 `input` / `script` / `output` / `evidence` 之一 → 它是 **role**
  * 整段的所有空白分隔 token **全部**形如 `k=v` → 它们是**属性**
  * 其余 → 拼进**说明**（多段用 ` | ` 接回去）

所以下面这些都合法，段序随意：

```
path: /blue/<组>/<用户>/data/agnews-clean | 去重后的训练集，12 GB
path: /orange/<组>/pockets | output | 纯 RNA 口袋 | n=4554 size=620756992 md5=7d4e1a9c checked=2026-08-09
path: /blue/<组>/cif_files | input | 原始 CIF | size=61203283968 missing=2026-08-09
path: s3://bucket/exports/run042.parquet | 导出的预测结果
```

**现存的 `位置 | 说明` 一个字都不用改。**第一行那段说明有中文有逗号，既不是 role
也不是纯 `k=v`，自然落进说明，读回来和以前逐字一样。

**「全部 token 都是 `k=v`」这条判据是刻意选严的**：`| lr=3e-4 的那次运行` 里只有
一个 token 形如 `k=v`，整段落进说明——**顺手在说明里写个等号不会被误当成机器字段**。
反过来，一整段全是 `k=v` 就几乎不可能是人写给人看的话。宁可漏认（掉回说明，
一个字不少），不可错认（人写的那句话从界面上消失）。

属性名必须以 ASCII 字母开头（`位置=这里` 这种中文键不会被当成机器字段），
值里不许有空白和竖线（那会把一行劈开）。

**一处已知的、可接受的代价**：`path: /x | output` 里那段说明恰好是 `output` 这个词时，
它会被读成 role 而不是说明。四个 role 词都是英文单词，中文说明不会撞上；
真要说明就多写一个字（`path: /x | 输出`）。

### 7.2 四个 role

| role | 这条路径对这一步是什么 |
|---|---|
| `input` | 吃进去的：数据集、上一步的产物、参考结构 |
| `script` | 跑这一步用的脚本 / 配置（代码仓请用 `code:`） |
| `output` | 产出的：权重、中间特征、结果表 |
| `evidence` | 佐证：日志、作业输出、审计留痕 |

不写 role 完全合法（现存记录全是这样）。role 只是让「这一步吃了什么、吐了什么」
能被机器读出来，猜错也不影响任何评级。

### 7.3 已知属性

| 属性 | 值 | 意思 |
|---|---|---|
| `size` | 非负整数，**字节** | 大小。存进文件的永远是字节数，「592 MB」是显示层现算的 |
| `n` | 非负整数 | 条目数（多少个文件、多少行） |
| `md5` / `sha256` | 校验和 | 拿到的还是不是当时那份 |
| `checked` | `YYYY-MM-DD` | **最后一次确认它存在**是哪天 |
| `missing` | `YYYY-MM-DD` | **最后一次确认它不存在**是哪天 |

**认不出来的属性照样解析出来并保留。**半年后有人写了 `nodes=12`，系统不该把它吃掉——
认不认得出来是程序的事，写下来的东西是人的事。

### 7.4 `checked` / `missing`：状态是派生的

`checked=` 和 `missing=` 都是**存储的事实**（像 `repro:`：某人某天真去看过一眼）。
「现在还在不在」是**派生**的，不写进文件：

- 只有 `checked=` → 存在
- 只有 `missing=` → 不存在
- 两个都有 → **看日期，晚的说了算**（先存在、后被清掉是最常见的时间线；
  反过来「清掉了又被重建」也一样成立）
- **同一天两个都有 → 判不存在**。这一整条需求的来历就是「三个目录已被删除、
  其中一个 57 GB，本该自动发现」——漏报一次丢失比多报一次警报贵得多
- 两个都没有 → 从未核对过

**最要紧的一条：服务端够不到 ≠ 路径不存在。**`/blue/` `/orange/` 只有在超算上才看得见，
S3 要有凭证，NAS 会掉线。查不到的时候**什么都不要写**——`missing=` 的语义是
「真去看过，确实没有」，不是「我这儿看不见」。把「查不了」写成「没了」，
得到的是一条**看起来像证据**的假结论，比没有结论有害得多。
（`agents/trace-auditor.md` 把这条列成硬规矩：✓ 在 / ✗ 没了 / ? 查不了，三种结果必须分开报。）

**路径没了不删记录。**「这份数据当年在这儿，现在没了」是溯源结论，和 `dead` 一样有价值。
`size=` 即使在 `missing` 时也留着——「没了的那个有 57 GB」正是要留下来的信息。

### 7.5 `code:`：代码在哪

```
code: <kind> | <位置> | <k=v …>
```

`kind` 有三种：

| kind | 什么时候用 | 位置写什么 |
|---|---|---|
| `git` | 代码在 git 仓库里 | 仓库地址；只有 commit 时可以留空，属性写 `commit=` / `rev=` / `tag=` |
| `snapshot` | 代码不在 git 里：超算上直接改的脚本，跑完打一个快照目录 | 快照目录。属性常写 `manifest=MANIFEST.md5` 和 `n=`（文件数） |
| `container` | 跑在容器里 | 镜像地址，或属性 `digest=` |

```
code: git | https://github.com/你/仓库 | commit=c1d2e3f
code: snapshot | /orange/<组>/run_snapshots/20260809 | manifest=MANIFEST.md5 n=43
code: container | ghcr.io/你/镜像:2026-08 | digest=sha256:9b7d112…
```

**`commit: c1d2e3f` 继续支持，它等价于一条 `code: git`**——但那条是**派生**出来的，
文件里永远只有 `commit:` 一份，绝不会再写一行 `code: git`。同一个事实存两处正是
上一代系统的死因。

**为什么要有 `code:`。**只认 `commit` 等于逼着「代码不在 git 里」的记录永远停在 L1，
而它们其实是找得回来的：快照目录 + 逐文件校验和，在可溯源性上不比 commit 差。
第 10 节的 L2 判据因此从「记了 commit」改成「**有任何一条 `code` 记录定位得到**」。

认不出的 `kind`（十年后多出 `nix`、`ipfs`）不丢也不报错，只要记了位置就算定位到了。

## 8. 删除

删除是「只追加」的一处**有意例外**，只用于**这条记录本身就不该存在**：
误建、测试数据、不小心粘进去的令牌或敏感信息。

**它和 `dead` 是两件事。** `dead` 是研究结论——试过、走不通——是这套系统里
最有价值的信号。往里塞垃圾会毁掉它；反过来，把真实的失败删掉，等于抹掉后来人
最需要的那条线索。

删除必须写原因（不写直接拒绝），原因会记进项目 `project.md` 的 `## 已删除` 小节，
格式见第 11 节——目录一删，那句话就是唯一留下来的东西。

**顺序是「先记原因，再删目录」。** 反过来的话，删到一半失败（Windows 上文件被
杀软 / 云盘 / 预览窗格占用极其常见）留下的就是最坏的组合：步骤从树上消失了、
残骸还在、而「为什么删的」一个字都没留。真删到一半时那一行会被就地标注成
`⚠ 删除未完成，目录里还有残留：…`，并**保留**——记录不完整好过既毁记录又不留痕。

三个明确接受的代价：

- **id 可能被重用**：删掉最高号之后，下一步会拿到同一个号。旧笔记里的「见 002」
  可能指向另一个东西。要避免就得有个用过哪些号的中心文件，而那正是 P1 禁止的。
- **子步骤会变孤儿**：它们的 `parent` 指向一个不存在的 id，会被降级为根并给出警告。
  构建不会崩，但那条线断了。
- **正文里的 `[[006]]` 会指向空处**：软链接不会被改写，它变成一句悬空的引用。
- **别的步骤的 `input: 006` 会指空**：数据依赖这条边（第 6 节）同样会被打断，
  而且后果比上一条重——`[[006]]` 是给人读的一句话，`input:` 是「这些字节从哪来」
  的声明，可溯源性正沿着它上溯。再叠上第一条（id 会被重用），下一个拿到 006 的
  步骤会**静悄悄地**接手这些边，那时它指向的是一个完全不相干的实验。

工具每次都会把这四件事各自发生了多少报出来（`orphaned` / `dangling_refs` /
`dangling_inputs` / `files_removed`）。删之前先读一眼。

## 9. 复现记录

```
repro: verified | 2026-08-08 | agent:claude | 干净 split 重跑 3 个种子，0.9506±0.0008
repro: failed   | 2026-07-02 | human        | /orange 上的 checkpoint 已被清理，跑不了
```

格式是 `状态 | 日期 | 谁 | 说明`，四段用竖线分开，说明里再有竖线也没关系（并进说明）。

三种结果：`runnable`（查过，命令/环境/种子齐全）、`verified`（真跑过，数字对上）、
`failed`（试过，跑不起来或对不上）。别的词一律拒绝。

**只追加不覆盖**：去年失败、今年成功是两条事实，后者不该抹掉前者。
`failed` 和成功一样要留着——「checkpoint 被清了」本身就是溯源结论。

工具层面这条只追加是**强制**的：MCP 的 `trace_update_step(repro=…)` 和 REST 的
`PATCH … {"add_repro": …}` 都只追加，没有「整组替换 repro」这条路。手写 note.md
时自己守住——想更正一条旧记录，追加一条新的并在说明里写清楚，别把旧的删掉。

抬等级的是**最后一条**（文件里最下面那一条），不是「历史上最好的一条」：
`verified` 之后又追加一条 `failed`，等级就跟着掉下来——那正是你想看见的。

---

## 10. 可溯源性等级

`check` 和网页会给出等级。前三级从文件机械算出，后两级要有人真去看过：

| 级 | 含义 | 判据 |
|---|---|---|
| **L0** | 不可溯源 | 「为什么」或「做了什么」空着，或有图没图注，或 done/dead 却没结论 |
| **L1** | 可读 | 上面这些都齐了——看得懂当时的判断，但跑不了 |
| **L2** | 可定位 | L1 + **代码找得回来** + 记了产物位置 |
| **L3** | 可重跑 | **已经到 L2**，且有人确认过命令/环境/种子齐全（`repro: runnable`） |
| **L4** | 已复现 | 真跑过，数字在容差内对上（`repro: verified`） |

**L2 的「代码找得回来」有三条路**（任意一条成立就算），判据的权威实现是
`trace_core.code_located`：

| 写法 | 什么时候算定位到了 |
|---|---|
| `commit: c1d2e3f` | 一直支持。等价于一条 `code: git`（派生，文件里只存一份） |
| `code: git \| <仓库> \| commit=…` | 记了仓库位置，或记了 `commit=` / `rev=` / `tag=` |
| `code: snapshot \| <快照目录> \| …` | **记了目录位置**。快照目录是必须的，没有位置不算数 |
| `code: container \| <镜像> \| digest=…` | 记了镜像位置或 `digest=` |

**有没有 `manifest=` / 逐文件校验和不再多分一级，这是有意的。**L2 的语义是
「**可定位**」——东西在哪记下来了；快照目录的路径本身就回答了这个问题。
校验和回答的是另一个问题（拿到的还是不是当时那一份），那属于「有人真去看过、跑过」
的 L3/L4。硬塞进 L2 会造出一个机械判不清的半级，而**阶梯一旦开始撒谎就没人看了**。

三处容易踩空的细节：

- `runnable` 只能把 **L2** 抬到 L3。一条 L0/L1 的记录挂上 `repro: runnable`
  还是 L0/L1——连命令都没写全，说「查过、跑得了」不成立。
- `verified` 无条件给 L4。真有人重跑出同样的数字，那这条记录事实上就是可复现的，
  哪怕正文写得潦草。潦草这件事仍然会以 `missing` 列表的形式列出来。
- `wip` 不要求「结论」（还没跑完），`done` / `dead` 要求。

**等级受依赖制约，而依赖 = `parent` ∪ `input`。**001 没记数据在哪，004 就算自己
写得再全，「004 这个结论是怎么来的」依然追不到底——整条链只能算 L1。
数据依赖同样算数：016 的输入来自 013，013 什么都没记，016 的整链等级就被 013 压住，
**哪怕 013 根本不在 016 的 `lineage` 里**（详情面板会说明最弱一环是从哪条边找到的）。
平局时依赖赢过自己，依赖之间 `parent` 先于 `input`。

所以补记录要**从最弱的那一环补起**，不是从最新那一步补起。

**还有第三个等级：整条定稿流程的等级**（第 16 节）。它同样取「最弱的一步」，
但范围是流程的成员，而且取的是每个成员**自己**的等级而不是它的整链等级——
整链会把被剔掉的 `dead` / `exclude` 祖先算进来，而那些步骤按定义就不是方法的一部分。
写论文时该报的是这一个：它回答的是**别人能不能重做出这个结果**。

**这两个等级在哪看得到：**

| 出口 | 看到什么 |
|---|---|
| `python trace_cli.py check` | 自身 / 整链两份等级分布，点名最弱的那一环并逐条列出它缺什么。`--strict` 把这些内容层缺陷也算成失败（给 CI 用），默认只有结构性错误才非零退出 |
| 网页详情面板「可溯源性 · L0–L4」 | 自身等级、整链等级、最弱一环（可点跳过去）、missing 待办清单、从根到自己的等级链条、全部 `repro` 记录含 `failed` |
| 网页图 / 列表 | 自身 L0 的节点标 `L0`，最近一次复现失败的标 `↺✕`。同一个字形通道上还有三个和等级无关的记号：`⇢n`（这一步有 n 个数据来源**不在树上**，见第 6 节）、`⇄`（被移动过，见第 14 节）、`⊘n`（有 n 处记下来的位置已确认不存在，见 7.4） |
| 网页数据流视图 | 画的是声明出来的 `input:` 那张 DAG，不是树。图例三档：只有树边 / 只有数据边 / 两者重合——**重合的占绝大多数**，不给它单独一档，读者会以为树边画丢了 |
| MCP `trace_read(project, step)` | 等级 + 逐条 missing |

`check` 还会把**内容层缺陷**报成警告：`missing_why` / `missing_what` /
`missing_conclusion` 只对 `done` / `dead` 生效（`wip` 是「还在写」，对着刚建出来的
空模板报警只会训练大家忽略警告）；`figure_without_caption` 任何状态都报。

**另有十条 `warn` 级提示不影响 L0–L4**，别把它们和上面那几条混起来看：
`section_without_prose` / `table_without_explanation` / `code_without_explanation`
（写法诊断，见第 3 节）、`dangling_input` / `self_input` / `input_cycle`
（数据依赖，见第 6 节）和 `lone_alternative` / `fork_without_decision` /
`undecided_fork` / `decision_without_candidates`
（分叉，见第 15 节）。它们出现在同一份 warnings 列表里，靠 `code` 区分。

---

## 11. `project.md`：项目洞察与删除记录

每个项目目录下有一个 `project.md`，和步骤的 `note.md` 是**两种不同的文件**：

```
projects/<项目 slug>/
├── project.md          ← 本节讲它
└── steps/
    └── 007_加入标题字段/
        └── note.md     ← 第 2–10 节讲它
```

### 它装什么

**项目级的沉淀。**「回译在这个数据集上一直没用」是三次尝试之后的判断，
挂在哪一步都不对，所以它在这里。规矩很简单：

- 只属于某一步的事 → 写进那一步的 `note.md`
- 跨若干步才成立的结论、会反复咬人的坑、还没验证但值得记的想法 → 写进 `project.md`

### 格式

```markdown
---
name: 我的课题
result: 023 | 亲和力预测：AUC 0.91（论文图 3）
result: 031 | 图 4 的消融
---

## 核心想法
- `p1` 先用小模型摸清上限，再决定要不要上大的

## 有效
- `p2` 训练集去重之后所有模型都涨 0.4 个点，这一步值得放在流水线最前面

## 无效
- `p3` 回译在这个数据集上一直没用，三次都在噪声内 —— 见 [[004]]

## 坑
- `p5` PDBFixer 误杀 944 个带修饰残基，见 [[013b]] · 取代 p4
- `p4` PDBFixer 误杀 1,099 个
- tokenizer 版本不一致会静默改分词，指标对不上时先查它

## 已删除
- `009` 误建的测试步骤 —— 粘错了令牌（2026-08-08 · agent:claude）
```

front-matter 有三个键：

| 键 | 说明 |
|---|---|
| `name` | 项目的**显示名**。**目录名（= URL 里的 slug）永远不动**——改显示名不改目录，已经发出去的链接不会失效 |
| `lang` | 可选。这份笔记**自己**是什么语言，和 `note.md` 的 `lang:` 同一个意思（第 13 节） |
| `result` | **哪一步是成果。可以重复多行，一个成果一行**，格式 `<步骤 id> \| <这是什么成果>`。定稿流程从它算出来，见第 16 节 |

`result:` **刻意不在 `trace_core.MULTI_KEYS` 那张表里**——那张表列的是 `note.md`
的可重复键（第 2 节），而 `result:` 是 `project.md` 的键。它由
`trace_core.parse_results` 自己扫 front-matter 的原始行读出来，所以「可以重复」
这件事对它照样成立。

写回文件时这三个键的顺序也是固定的（`name` → `lang` → `result…`），
`result:` 之间保持你声明的先后——那也是导出里成果出现的顺序。

正文的小节标题是**精确匹配**的，一个字都不能差：

| `## ` 标题 | `trace_insight` 的 `kind` | 写什么 |
|---|---|---|
| `核心想法` | `idea` | 还没验证、但值得记下来的想法 |
| `有效` | `works` | 试过、确实管用的做法 |
| `无效` | `fails` | 试过、不管用的做法（和 `dead` 一样有价值） |
| `坑` | `pitfall` | 会反复咬人的陷阱 |
| `已删除` | —— | 删除审计，**由系统自己写**，见下 |

每条洞察是**一行**，`- ` 开头。写不下就精简，不要折行——它要能被 `grep` 一行捞出来。

小节的先后顺序不重要（工具按标题找，找不到就在末尾补一个），
缺哪个小节就没有哪个，不需要预先把四个空标题都摆上。

### 洞察的 id 与「取代」

一条洞察可以带一个 id，写在**行首的反引号里**（和 `## 已删除` 里 `` `009` `` 同一个写法）：

```markdown
## 坑
- `p5` PDBFixer 误杀 944 个带修饰残基，见 [[013b]] · 取代 p4
- `p4` PDBFixer 误杀 1,099 个
```

规矩：

- id 的形状是 `[A-Za-z][A-Za-z0-9_-]{0,15}`（权威定义是 `trace_core.INSIGHT_ID_RE`，
  读写共用同一条）。工具分配的是 `p1` `p2` `p3` …，取 **max+1，不重用**
- **id 在整个 `project.md` 内唯一，不分小节。**一条洞察会从「坑」变成「无效」，
  id 跟着小节走就等于换一个 id，而笔记里「见 `p4`」这样的引用要一直有效
- id 跨原文和**全部译文**一起分配（`project.en.md` 里的 `p7` 会占掉 `p7`），
  所以中英两边说的 `p4` 永远是同一条洞察
- **没有 id 的旧行照常工作**，现存手写的洞察全是这样，不需要回填

**重锚**（更正措辞、补上出处、加上「取代了谁」）就是按 id 就地改那一行，
不新增也不删除。

**取代**：`· 取代 p4`（英文译文里写 `· supersedes p4`，词表和小节名同一套，
见第 13 节）。三条硬规矩：

1. **只写在取代者身上。**「p4 已被取代」是**派生**的——磁盘上只有 p5 那半句话，
   p4 那一行一个字都不改。写第二份就又是双真相源，而且它一定会先漂移
   （人只会去改自己正在写的那一行）。
2. **被取代的那条不删。**「先数出 1,099 个、查清楚之后是 944 个」是两条事实，
   后者不该抹掉前者。界面上被取代的那条**折叠显示**（默认收起、可展开），
   磁盘上它一直在那儿，`grep` 得到。
3. 一条可以取代多条：`· 取代 p1, p2`。

**为什么值得有 id。**没有 id 时，更正一条洞察只有两条路：把整段正文重贴一遍
（会连带覆盖别人刚写的），或者再追加一条相似的（于是同一个坑在文件里躺着三份，
读的人不知道该信哪个）。id 让「就地改这一条」和「这一条被那一条取代了」都成为
可以写下来、也读得回来的事实。

### 「已删除」小节

这一节由 `delete_step` 自己写，格式是：

```
- `<id>` <被删步骤的标题> —— <删除原因>（<日期> · <谁删的>）
```

**永远不要手工删掉这一节里的内容。**目录已经没了，这一行是「006 是什么、
为什么删的」仅存的证据——也是 G4（删掉全部程序后 `grep -r` 仍能回答
「我当年为什么放弃了 X」）在删除这条路上唯一的兑现。

工具这一侧已经上了闸门：会整体替换洞察的那两条路（网页的「✎ 编辑洞察」、
`PATCH /api/projects/{项目} {"insights": …}`）**只替换上面那四个洞察小节**，
`## 已删除` 以及任何其它小节都逐字保留；就算你把整段正文连 `## 已删除`
一起贴回去，提交的那一份也会被丢弃，磁盘上的那一份赢。
`trace_insight` 本来就只往对应小节追加一条，碰不到别的地方。

`trace_insight` 的可选参数 `step` 会在这条洞察末尾接上 `—— [[004]]`，
指回它的证据来源。**养成带上它的习惯**——半年后「这个结论是从哪来的」全靠它。

### 手写它要注意什么

`project.md` 和 `note.md` 一样，必须是 **UTF-8**。不是的话工具会拒绝改它并让你先转码
——因为定死按 UTF-8 回写会把原文替换成一串 `�`，那才是不可逆的损失。
（Windows 上 `cmd.exe` 的 `echo … > project.md` 是 GBK，PowerShell 5.1 的 `>` 是
UTF-16LE，两个都要避开。）

---

## 12. 渲染器支持的 markdown 子集

网页用的是仓库自带的极简渲染器（`web/md.js`，零依赖、不走 CDN、`file://` 下可用）。
它**不是** CommonMark 的完整实现。按标准写记录之前先知道下面这张表，
免得写出一段渲染器读不出来的语法——你（agent）看不到渲染结果，不会当场发现。

**支持：**

| 语法 | 备注 |
|---|---|
| `#`–`######` 标题 | |
| 段落、`---` 分隔线、`>` 引用 | |
| ` ```lang ` 围栏代码块 | info string 可带参数；**列表项里也能放**，不会把列表劈开 |
| `` `行内代码` `` | |
| 无序 / 有序 / 任务列表 | 任意层嵌套；有序子列表保持编号（认 `start=`）；项间空行不切断列表 |
| `**粗体**` `*斜体*` `__粗体__` `_斜体_` `~~删除线~~` | `_` 两侧要求非词内字符，`some_var_name` 不会被劈开 |
| 表格 | 列对齐 + 数值列自动右对齐 + 底纹条（见第 4 节）；`\|` 是字面竖线 |
| `[文字](目标)` `![alt](图 "图注")` | 目标可用 `<尖括号>` 包住（含空格时必须），也认括号成对的裸路径 |
| 裸 URL、`<https://…>` 自动链接 | 括号成对，`.../Foo_(bar)` 不会被截断 |
| `[[007]]` | 内部跳转 + 对方页面显示反向链接（这是本项目自己的扩展） |
| `\*` 之类的反斜杠转义 | 只对 ASCII 标点生效，`C:\Users\…` 原样保留 |
| `$…$` / 独占行的 `$$…$$` | **不渲染成公式**，见下 |

**有意不支持**（写了就是原样一坨文本，别用）：

- 引用式链接 `[a][ref]` 和它的定义行
- 脚注 `[^1]`
- setext 标题（`标题` 下面一行 `===`）
- 四空格缩进代码块（用 ` ``` ` 围栏代替）

**数学公式的处理方式要单独说。** 系统**不引入 KaTeX / MathJax**（那会是一个 CDN
依赖，和「零依赖、`file://` 下可用」冲突）。`$…$` 和 `$$…$$` 的做法是
**原样抠出来当不可变文本**：定界符、反斜杠、下标花括号一个字符都不动，
只是标上 `class="math"` 用等宽体显示，且不会被 `*` `_` 之类的行内规则啃掉。

```markdown
损失是 $\mathcal{L} = -\sum_i y_i \log \hat{y}_i$，实测收敛到 0.31。

$$
\mathrm{AUC} = \frac{1}{|P||N|} \sum_{p \in P} \sum_{n \in N} \mathbf{1}[s_p > s_n]
$$
```

所以人和 LLM 读到的**都是原式**——正合第 0 节那条总原则。反过来说：
**指标不要写成公式**，写成第 4 节的表格；公式只用来说清定义。

判 `$` 的规则跟 pandoc 一样：收尾的 `$` 后面不能紧跟数字，所以
「花了 $5 又花了 $3」不会被误当成一段公式。

---

## 13. 双语：`note.<lang>.md`

一条记录可以有多个语言版本。存储是**双文件**——结构只存一份，译文只带正文：

```
007_加入标题字段/
├── note.md            ← 结构 + 主语言正文（唯一权威）
├── note.en.md         ← 只有 title 和译文
└── loss_curve.png     ← 附件
```

`note.md`：

```markdown
---
id: 007
parent: 005
status: done
title: 加入标题字段，准确率 0.943 → 0.951
lang: zh
commit: c1d2e3f
---

## 为什么
基线的 TF-IDF 完全丢掉词序。
```

`note.en.md`：

```markdown
---
title: Add title field, accuracy 0.943 → 0.951
---

## Why
The TF-IDF baseline discards word order.
```

### 文件名

`note.<lang>.md`，和 `note.md` **同一层**。判定用的正则就是这一条：

```
^note\.([A-Za-z][A-Za-z0-9-]{0,34})\.md\Z
```

- 语言码是**短语言码**：`en` / `ja` / `zh-Hant` / `zh-TW`。字母开头，之后只能是
  字母 / 数字 / 连字符，总长不超过 35
- 大小写按 BCP-47 的惯例写（语言小写、四字母的文字段首字母大写、两字母的地区段大写）。
  走工具写时会自动归一（`EN` → `en`，`zh-hant` → `zh-Hant`），于是「一种语言 ⇒ 恰好
  一个文件名」在 NTFS（大小写不敏感）和 ext4（敏感）上都成立
- 语言码不能是 `md`：那会造出一个看起来像备份、实际会被当成「一种叫 md 的语言」的文件
- **只看本层**。嵌套目录里的 `note.en.md` 是普通附件，不是这一步的译文
- 反过来，本层的译文**不是附件**：它不出现在附件清单里，网页上删不掉（删掉它等于
  悄悄删了整个英文版正文）。删掉整个步骤时它跟着一起删

项目笔记同理：`project.<lang>.md`，和 `project.md` 同一层。

### front-matter 里只准有 `title`

项目笔记是 `name`。除此之外，这十八个结构键写进翻译文件会被**读都不读地丢掉**，
并各产出一条 `translation_structural_key` 警告（显示在网页顶栏，`check` 里也有）：

`id` · `parent` · `status` · `date` · `commit` · `author` · `tags` · `path` · `repro` · `key` · `input` · `code` · `moved` · `branch` · `decision` · `result` · `pipeline` · `chapter`

`branch` / `decision`（第 15 节的分叉语义）和别的结构键是同一条理由，只是更隐蔽：`branch:`
说的是「这条**边**是什么意思」，`decision:` 说的是「这个**岔路口**在问什么」——
两者都是结构，不是正文。译文里各写一份，同一棵树就会按两套边被画成两个版本。
「在决定什么」那句话确实是人写的自然语言，读起来很像该翻译的东西，但它现在
**没有译文通道**：要让它可翻译得先有一次单独的设计决定（`TR_ONLY_KEYS` 目前只有
`title`），在那之前请写在 `note.md` 里。

倒数第三、第二个（第 16 节的 `result:` / `pipeline:`）后果更重：它们决定**哪些步骤进定稿流程**。
译文里各写一份，中文页面和英文页面会导出两条不同的 Methods，而两边看着都像对的——
那正是双真相源最难查的一种。竖线右边那半句说明当然可以翻，但它跟着整份
`project.md` / `note.md` 的译文走，不是靠在译文里重写一遍 `result:`。

**最后一个（第 17 节的 `chapter:`）是这一串里最能悄悄分家的一个**，因为它**沿树继承**：
译文里多写一行不是「这一步换了个章节」，而是**整条子树**在英文页面上换了归属，
于是同一个项目按两种分法各导出一份 Methods。章节名本身也不该在译文里改写——
它是靠**同名**成立的身份（第 17 节），`grep -r "chapter: 消融实验"` 要原样捞得到（G4）。
界面上想把「消融实验」显示成 Ablation 是**界面文案**的事，写进文件就成了第二份声明。

**为什么这么硬。** 上一代系统（ai-training-logbook）的死因是**双真相源**：
父子关系同时存在于两个地方，写一处漏一处，页面上永远有一半是错的。
`note.en.md` 里的 `parent: 006` 如果能生效，同一个错误原样回来，而且更隐蔽——
两份值平时看着都对，只有改了其中一份才炸。

所以这里不是「note.md 优先」这种**合并**策略（合并意味着两边都是来源），
而是**只有 `note.md` 是来源**，译文里的结构键连读都不读。

同一条理由的另一面：`trace_new_step` / `trace_update_step` **没有** `body_en`
这类参数。原文和译文各走各的写入路径，译文那条路碰不到 `note.md` 一个字节。

一个要知道的不对称：**项目笔记的译文（`project.<lang>.md`）里的结构键同样被忽略，
但那一侧没有警告通道**——不会有任何提示，只是静静地不生效。步骤的译文
（`note.<lang>.md`）才会把警告一路送到网页顶栏和 `check`。手写项目译文时自己守住。

### 主语言：`lang:`

`note.md` 的 front-matter 可以写 `lang: zh`，声明**这份正文自己**是什么语言。
不写就是「没声明」——系统**不猜**（不做字符集探测，不做语种识别）。
`project.md` 同样认这个键。

它有两个用处：网页知道正文本身是什么语言（切到该语言时不必去找译文），
以及写入侧据此拒绝「给一份已经声明 `lang: en` 的记录再写一份 `en` 译文」——
那两份会各说各话，正是这套设计要绕开的坑。

它可以手写（`vim note.md` 加一行，P1 之下完全合法），也可以走写入接口：
`trace_new_step` / `trace_update_step` 都收 `lang` 参数，`PATCH … {"lang": "en"}` 同理。
改它**只改这条声明**，不会去动正文，也不会碰任何一份译文。

### 小节名对照表（**逐字照抄**）

翻译文件要能被同样地解析、同样地评级，靠的是一张**封闭词表**。
写译文时小节标题必须用目标语言的那一套，一个字都不能差：

| 语义键 | 中文 | 英文 |
|---|---|---|
| `why` | `## 为什么` | `## Why` |
| `what` | `## 做了什么` | `## What` |
| `result` | `## 结果` | `## Result` |
| `conclusion` | `## 结论` | `## Conclusion` |
| `next` | `## 下一步` | `## Next` |
| `idea` | `## 核心想法` | `## Ideas` |
| `works` | `## 有效` | `## Works` |
| `fails` | `## 无效` | `## Doesn't work` |
| `pitfall` | `## 坑` | `## Pitfalls` |
| `deleted` | `## 已删除` | `## Deleted` |

前五个是步骤正文的小节（第 3 节），中间四个是项目笔记的洞察（第 11 节），
最后一个是删除审计——它由系统自己写，你只会读到，不该手写。

写成 `## Why not` 或者 `## Why（承接上一步）` 就等于没写：评级和 `check`
就是按这几个名字去正文里找内容的。

**封闭词表目前只有 `zh` 和 `en`。** 别的语言（`ja` / `de` / …）照样存得下、
`grep` 得到、界面显示得出，但它的小节标题不在表里，评级和 `check` 读不出内容——
那份译文对「写了没有」的判定不产生任何贡献（也不会让等级掉下来，原文写了就够了）。
要让某种语言参与评级，得先把它加进 `trace_core.SECTION_NAMES`。

### 评级：任一语言写了就算写了

L0–L4 和 `check` 的 `missing_why` / `missing_what` / `missing_conclusion` 问的是
**「这个判断有没有被记下来」**，所以只要 `note.md` 或**任何一个**译文里写了，就算写了。
只写了中文的记录照样是可溯源的；只写了英文的也是。

把「没翻译」算成缺陷会得到一个「每加一门语言就集体掉级」的评级——那等于惩罚翻译。

**图注是唯一的例外，逐个文件独立判。** 中文版写了图注、英文版只有一个光秃秃的
`![](loss_curve.png)`，读英文版的人（以及只被喂了英文版的 agent）拿到的就是零信息。
所以译文里漏图注会真的把这一步的等级压下去，警告的 `where` 指到具体文件：

```
007_加入标题字段/note.en.md   figure_without_caption
```

一句话说清这个差别：小节问的是「这个判断有没有被记下来」（记在哪一份里都算记下来了），
图注问的是「这张图对**正在读这一份文件的读者**说了什么」（另一份写了不解决问题）。

### 项目笔记 `project.<lang>.md`

同样的规矩：front-matter 里只准有 `name`，四个洞察小节按上面的对照表翻。

```markdown
---
name: My topic
---

## Ideas
- Probe the ceiling with a small model first, then decide whether to scale up

## Doesn't work
- Back-translation never helped on this dataset, all three tries were within noise —— [[004]]
```

`## 已删除` / `## Deleted` 由系统自己写（`delete_step` 只写 `project.md`），
和中文版一样**永远不要手工删**。整体替换洞察的那条路对译文也只替换那四个洞察小节，
译文里手写的 `## Deleted` 逐字保留，提交文本里的同名小节直接丢弃。

### 「还没翻译」是派生状态

一步有没有英文版，看 `note.en.md` 在不在——和「有几个子节点」「有哪些附件」一样，
是**扫出来的**，不是存下来的。没有任何地方存着一张「待翻译」清单。

于是：**缺翻译不报警告、不加徽标、不算缺陷**。它只在界面切到某个语言而这一步
没有那个语言版本时，由网页如实说明并回退到原文——那是必要的提示，不是警告。
想知道还欠哪些，用 `trace_untranslated` 现算一份。

### 怎么写

**用 `trace_translate`，不要手工创建文件。**

```
trace_translate(project="我的课题", lang="en", step="007",
                title="Add title field, accuracy 0.943 → 0.951",
                body="## Why\nThe TF-IDF baseline discards word order.\n")

trace_translate(project="我的课题", lang="en",              # 省略 step = 翻项目笔记
                title="My topic", body="## Ideas\n- …\n")
```

- `title` 走参数，**不要自己在 `body` 里拼那段 `---`**。翻译文件的 front-matter
  由渲染器生成，它只认得一个键，所以结构键从函数形状上就注不进去
- `expect` 是乐观并发控制，对的是**译文自己**的 digest（`sha256(note.en.md 原始字节)[:12]`），
  不是 `note.md` 的。两条链各管各的
- 撤掉某个语言版本走 `python trace_cli.py tr -P <项目> --step 007 --lang en --drop`
  （REST 是 `DELETE …/steps/{id}/tr/{lang}`）。**不要写一份空的译文**——空文件会让
  界面认为「这一步已经有英文版了」，于是不再回退，读者看到的是一片空白

**系统不判断「翻译过时了」，这是刻意的。** 要判断就得把翻译当时 `note.md` 的指纹
存进译文，那是把派生关系变成存储字段（P1 禁止），而且这份文档鼓励人直接
`vim note.md`（页面五秒内自己跟上），手改一次那个指纹就变成一句谎话，
系统却会理直气壮地说「已是最新」。退而用 mtime 更糟：`git checkout`、`rsync`、
云盘同步、从备份恢复都会重排 mtime，于是最常见的几种操作之后集体误报。
真需要这个信息，就在译文里自己写一句——那是可 `grep` 的文本，G4 成立。

手写也合法（P1 之下一切都是文件），但要自己守住四件事：文件名的形状、
front-matter 里只放 `title`、小节名照抄对照表、UTF-8。

### 界面文案是另一回事

这一节讲的是**记录内容**的双语。网页**界面文案**（按钮、标签、提示、L0–L4 的说明）
是另一套东西，存在 `web/i18n.js` 里，默认英文、可切换，和记录本身用什么语言无关。
`FORMAT.md` / `README.md` / MCP 工具描述 / Python 侧的报错文案都保持中文。

### G4 在双语下仍然成立

删掉全部程序之后：

```bash
grep -rn "放弃" projects/          # 中文版里的那句结论
grep -rn "abandoned" projects/     # 英文版里的同一句
```

同一步的同一条结论，两种语言各有一份**纯文本**，各自都能回答「我当年为什么放弃了 X」。
双语没有引入任何需要程序才能解开的东西——多的只是同一个目录里多一个 `.md`。

---

## 14. `moved:`：`parent` 可以改，但要留下审计

### 只追加的地基是「不丢历史」，不是「不能改结构」

**记下来就不丢。**这份标准以前写着「父子关系一旦写下不再改」，那条已经不成立了。
现在的规矩分成两半：

| | 能不能改 | 为什么 |
|---|---|---|
| `id` | **不能改，也不重发** | 笔记里的 `[[003b]]`、论文脚注里的引用要**永远**有效 |
| `parent` | 能改，但每改一次必须追加一条 `moved:` | 树形本来就可能画错；改动留了痕，历史反而更完整 |

**为什么这个改动是对的。**没有移动能力的时候，人被逼出来的是更坏的东西：
一整条支挂错了父节点，于是把**两个节点的正文对调**。结果是 013b 的创建日期和它现在
装的内容对不上号，`[[013b]]` 指向的东西悄悄换了一个，而这次修改**一条记录都没留**。
能移动 + 强制写原因，历史反而完整。

### 格式

```
moved: 2026-08-09 | 014 | 013b | human | 补原子的产物从未进过下游计算，树形是错的
```

五段：`日期 | 原 parent | 新 parent | 谁 | 为什么`。原 / 新为空表示根（落盘就是空段）。
原因里再有竖线也没关系（并进原因，和 `repro:` 同一个规矩）。

**可以重复多行，顺序即历史，只追加。**一个节点被移动过三次就有三行。

**为什么放 front-matter 而不是正文里的「## 已移动」：**正文是人的判断区，
一次编辑就可能整段误删；front-matter 是机器记录区，工具全量重写它，
这一行只会被追加，不会被人顺手改没。

### 约束

写入侧会挡下这几种（每一条都对应一个真会犯的错）：

- **`reason` 必填。**半年后看到一棵和创建顺序对不上的树（016 挂在 013b 下面，
  而它的号比 013b 大），唯一能解释它的就是这句话。写清楚是**哪条数据依赖**
  决定了新的父子关系，不要写「修正结构」。
- 目标必须**存在**且在**同一个项目**里。跨项目的关联请用正文里的 `[[引用]]`。
- 不能挂到**自己**或**自己的后代**下面——那会让整棵子树从森林上掉下来。
- 新 parent 和现在的一样直接报错：一条什么都没改的审计只会稀释真正的审计。

**移动带走整棵子树**，并且**不改变 `inputs`**。数据依赖是另一件事（第 6 节），
它本来就允许有多个来源；树形改对了，数据流该怎么写还是怎么写。

### 和「删除」的关系

第 8 节的删除是只追加原则**唯一的例外**（记录本身就不该存在）。移动**不是例外**：
它什么都没丢，只是把「挂在哪」这件事的历史也写了下来。

---

## 15. 分叉与汇回：树上的三种边

树上所有的父子边画出来长得一模一样，但它们说的不是同一件事。这一节把三种分开。

**判别只要问一句话：**

> **这两条能同时成立吗？**

| 回答 | 是哪一种 | 怎么写 |
|---|---|---|
| **不能** —— 同一个问题的两个答案，只能选一条走下去 | 互斥候选 | 每个候选各写一行 `branch: alternative` |
| **能** —— 接着上一步往下做，各做各的 | 普通延伸 | 什么都不用写（默认就是 `extends`） |
| 一条支线的**产物**后来参与了另一条线上的某一步 | 汇回 | 一行 `input:`，**不是** `branch:` |

第三种最容易被误写成 `branch:`。C 是从 A 延伸出来的（一条普通的 `extends`），
后来 C 产出的文件又被 A 线上更靠后的那一步吃掉了——树上 C 只有一个 parent，
那条「又回来了」的关系长在**数据流**上（第 6 节），不是第二条父子边。
它不需要任何新字段，`input:` 早就把它记下来了。

### 15.1 怎么写

分叉点说出**在决定什么**。这句话和「为什么」一样推导不出来，只能人写：

```markdown
---
id: 011
status: done
title: 类别不平衡的处理方式
decision: 类别不平衡怎么处理？两条路只能选一条走下去
---
```

每个候选**各自**声明「我是一个候选」，只写自己那一行：

```markdown
---
id: 012
parent: 011
branch: alternative | 先试最便宜的：只调采样权重
status: dead
title: 只调采样权重
---
```

```markdown
---
id: 012b
parent: 011
branch: alternative | 换 focal loss，改动大但可能更稳
status: done
title: 换成 focal loss
---
```

`branch:` 的取值只有两个：

| 取值 | 这条边什么意思 |
|---|---|
| `extends` | 我接着 parent 往下做。**默认，不写就是它** |
| `alternative` | 我是 parent 底下那个岔路口的一个候选，和同组其余候选只能选一条走下去 |

- 竖线右边是**这个候选自己的角度**（沿用整份 front-matter「竖线左边给机器、右边给人」
  的惯例），自由文本，可以不写：`branch: alternative` 一行就够
- 认不出的取值（`alterative` 这种笔误）报一条 `bad_branch` 并**退回 `extends`**——
  和 `status:` 同一条路：报一声、退回默认值、继续建树，一个拼错的词不该让这一步
  从图上消失。走写入接口时则当场拒绝（写入侧面对笔误就该问清楚）
- **默认值不落盘。**`branch: extends` 和不写是同一个意思，存后者等于把一个派生默认值
  写死进文件，也会让现存的每一条记录下一次编辑时集体多出一行 diff。唯一的例外是带了说明的
  （`branch: extends | …`）——说明是人写的字，丢了就没了
- `decision:` 写在**分叉点**（也就是候选们的父节点）身上，**不是**写在候选身上

### 15.2 「这一组有谁」是派生的，绝不在兄弟之间互相登记

一个节点底下所有 `branch: alternative` 的孩子构成**一组候选**。这一组有谁，是扫父节点
的孩子现算出来的（`trace_core.compute_branch_groups`），磁盘上**只有每个孩子自己那一行**。

**不要**在父节点上写一份候选清单（`options: 012, 012b`），也**不要**在兄弟之间互相
指名（`alt: 012b`）。理由和译文里不许写 `parent:` 是同一条：

- 互斥是一组关系，登记在兄弟之间要写 N×(N−1) 份，改一处漏一处；
- 存下来的清单一定会过期——一个候选被 `trace_move_step` 挪走之后，父节点上那份
  清单还理直气壮地列着它；
- 同一个事实存在两处，正是上一代系统的死因。

### 15.3 「选了哪个」靠 `status: dead` 表达，不另开字段

做出决定这件事，写下来就是**把其余候选标 `dead` 并写清放弃的理由**。所以没有
「选中了谁」这个字段，也永远不会有一个「标记赢家」的按钮——那个按钮就是第二份真相。

于是白拿一个这套系统最需要的派生信号（`trace_core.BRANCH_STATES`）：

| 一组候选里还有几个不是 `dead` | 状态 | 意思 |
|---|---|---|
| 恰好一个 | `decided` | 就是它了。「选了哪个」是从其余几条 `dead` 反推出来的 |
| 一个不剩 | `abandoned` | 这个问题的答案是「都不行」。**这是结论，不是错误**（P4） |
| 两个以上 | `open` | **这个岔路口还没做决定** |

最后一行是这整件事真正的收益：「我手上还有几个岔路口悬着」以前只能靠人记，
现在是扫出来的。

**根节点之间也成组**（这一组的「分叉点」是空的）：两条互斥的开局没有共同的父节点
可挂，不给它成组等于让那两行 `branch: alternative` 悄悄失效——写了却什么都不发生，
比报错更难查。这一组自然没有 `decision:`（没有节点能承载那句话）。

### 15.4 汇回边不用写，它就是 `input:`

判据、以及为什么祖先链上的和跨树的不算，见第 6 节「哪些 `input:` 边是汇回」。
一句话：**两端在同一棵树里，且谁都不是谁的祖先**。

接着 15.1 那个例子往下：011 分叉出 012 / 012b 两个候选，012b 这一支底下的 013
产出了一份打分表，而 012 那一支底下更靠后的 014 把它读了进去（014 的 front-matter 里）：

```
input: 013 | scores.csv
```

系统据此算出一条汇回边，并顺带算出两条线**分家的那个岔路口**（011）——
「013 的产物汇回到 014，两条线在 011 分开」这句话里的每一个 id 都是现算的。
写记录的人要做的只有一件：**真的读了那份文件，就把 `input:` 写上**。

### 15.5 四条 `warn` 级诊断（一条都不影响等级）

和第 3 / 6 节那六条一样，下面四条只提示、**不进 L0–L4**，一条都不会让等级掉下来。
它们问的是「这个岔路口讲清楚了没有」，不是「这个结果追不追得到」。

| `code` | 什么时候报 | 想说什么 |
|---|---|---|
| `lone_alternative` | 一组候选只有一个 | 一个候选不成其为选择。多半是另一条支漏标了，或者它其实是普通延伸 |
| `fork_without_decision` | 有候选，分叉点却没写 `decision:` | 候选有谁、选中了谁都算得出来，唯独「当时在决定什么」只能人写 |
| `undecided_fork` | 一组里有两个以上候选还活着 | **提醒，不是责备。**同时开几条线是研究的常态 |
| `decision_without_candidates` | 写了 `decision:`，底下却没有任何一个 `branch: alternative` | 那一行现在**什么都不做**：不成组、图上不画括弧、`forks` 清单里也不出现 |

最后那一条是 `fork_without_decision` 的**镜像**，而且更容易被读成 bug：写下的问题
在页面上看得见，可它周围什么都没有，最先想到的解释是「没保存上」。它报的是
「记下来了，只是还差候选」——给每条路各写一行 `branch: alternative` 它就立起来了；
要是这里其实没有分叉，把 `decision:` 那一行删掉。

`undecided_fork` 的措辞要当真：把「还没决定」说成错误，人就会为了让界面干净随手标一个
`dead`——那是拿一条假结论换一块绿色。等哪条真走通了，再把其余的标 `dead` 并写清
为什么放弃，这个岔路口才算结掉。

（根之间那一组不报 `fork_without_decision`：`decision:` 得写在分叉点上，而它没有
分叉点，报一条改不动的警告只会训练人忽略警告。）

### 15.6 图上怎么区分

三种边在网页上各有一种颜色，**并且各自还有一个非颜色的通道**：普通延伸是正交折线
（不变），互斥候选在分叉处多一道把这一组括起来的弧形括弧加「N 选 1 / 已定 / 都不行」
标注，汇回画成带箭头的曲线（树上的边从不弯）。所以灰度打印和色觉障碍下仍然分得清。
契约写在 [README](README.md) 的「三种边的视觉编码」一节。

---

## 16. 两条路径：开发路径（记录）与定稿流程（方法）

一个项目从来不是一条直线。十四步里真正产出那个数字的可能只有六步，其余八步是
试过不行的、是画了一眼就没再用的图、是还没结掉的岔路口。这两样东西**都要留**，
但它们回答的不是同一个问题，所以分成两条路径。

| | **开发路径** | **定稿流程** |
|---|---|---|
| 是什么 | 这棵树的**全部**，含 `dead`、含还没决定的岔路口 | 真正产出成果的那**一条链** |
| 回答 | **我当初是怎么走到那儿的** | **别人该怎么做** |
| 给谁 | 自己、查问题的人、半年后的自己 | 合作者、审稿人、论文的 Methods |
| 怎么来的 | 就是第 2–15 节讲的那棵树 | 从 `result:` 反向做闭包**算出来的** |

**为什么两个都要留。**只留开发路径，别人拿到的是十四步，得自己猜哪六步是流程
——猜错一步，复现出来的数字就对不上，而且不知道错在哪。只留定稿流程，半年后的
你自己会问「当初为什么排除了另一条路」，而那句话只写在被剔掉的那几步的
`## 结论` 里；把它们删掉，等于把 G4（`grep -r "放弃"` 还能回答问题）作废。

> 定稿流程**不是**开发路径的精简版，两者说的不是同一句话。
> 一个说「怎么走到那儿的」，一个说「该怎么做」。

### 16.1 只声明一件推导不出来的事：哪一步是成果

写在 `project.md` 的 front-matter 里（第 11 节），**可以重复，一个成果一行**：

```
---
name: 口袋筛选
result: 023 | 亲和力预测：AUC 0.91（论文图 3）
result: 031 | 图 4 的消融
---
```

竖线左边是步骤 id，右边是「这是什么成果」，自由文本——沿用整份 front-matter
「竖线左边给机器、右边给人」的惯例，和 `input:` 逐字同一个语法。

- 同一步声明两次没有意义（两个一模一样的闭包，两句不同的说明没人知道该信哪句），
  再写一次就是**改写那一行**，位置不动
- 指向不存在的步骤会报一条 `dangling_result`。它比悬空的 `input:` 严重得多：
  悬空的 `input:` 只是让一条数据边不参与计算，悬空的 `result:` 让整条流程从一个
  不存在的起点出发，**结果是空的**
- **一个 `result:` 都没声明是常态，不是缺陷。**没有它就推不出定稿流程，
  界面上说的是「怎么声明」，不是「你少写了东西」

### 16.2 其余全部是算出来的

**成员清单一个字都不存。**存下来就是一份会漂移的中心索引（P1 禁止）：你移动一步、
补一条 `input:`、把某一支标 `dead`，流程必须自己跟着变，而一份落盘的清单只会
理直气壮地列着已经不对的东西。所以父节点上没有清单，项目上也没有清单，
**永远不要去找那个「编辑流程成员」的输入框**。

推导规则一共四条（实现是 `trace_core.pipeline_deps` 和 `compute_pipeline`）：

1. **从每个 `result:` 出发，沿 `input:` 反向做闭包。**`input:` 回答的正是
   「这些字节从哪来」，所以它是首选。
2. **一步一条 `input:` 都没写时，退回它的 `parent`。**绝大多数步骤不写 `input:`
   ——不是因为没有输入，而是因为输入就是上一步，写出来是废话。真按「没写就没有
   上游」算，现存每一个项目的流程都只剩成果那一步。在一条直链上，「我接着上一步做」
   和「我吃了上一步的产物」说的是同一件事。
   （只要写了 `input:` 就**只**沿 input，哪怕那几条全是悬空的——那时闭包就在这里
   断掉，因为悬空已经由 `dangling_input` 报过了，再拿 parent 顶上等于替人猜来源。）
3. **闭包里 `dead` 的剔掉。**`dead` 是结论不是错误（P4），但一条被放弃的路
   本来就不该出现在「照着做」的说明书里。
4. **应用每一步自己的例外**（下一节的 `pipeline:`），以及一条压倒一切的规矩：
   **声明成成果的那一步永远在流程里**（否则这条流程连终点都没有）。

被剔掉的步骤**把上游接过去**，不留断口：020 是 `dead`、023 吃 020 的产物、
020 又是从 013 来的——剔掉 020 之后若把边一起丢掉，013 就成了一个和成果毫无关系的
孤点。013 的字节确实流进了 023（只是路上经过一段已经放弃的路），所以那条边照接，
并记下**路过了谁**（`via`）。图上该把这种边画成虚的或者标出来，
它也正是「从流程跳回开发路径」的锚点。

**显示顺序**是按数据依赖拓扑排序，平局按 id 序。平局必须有个说法，否则同一份文件
在两台机器上能排出两种顺序，而 Methods 的段落顺序会跟着变（P3 要逐字节确定）。
数据依赖成了环时不会死循环：报一条 `pipeline_cycle`，环上那几步按 id 序排在最后。

#### 在脑子里算一遍

一个六步的项目，`project.md` 里只有一行 `result: 023 | 亲和力预测：AUC 0.91`：

| 步骤 | `parent` | `input` | `status` | `pipeline` | 在定稿流程里吗 |
|---|---|---|---|---|---|
| `001` 抓下全部 CIF | —— | —— | `done` | —— | ✓ 沿 parent 一路退回够到的 |
| `013` 算口袋组成 | `001` | —— | `done` | —— | ✓ 023 吃的那些字节从这里来 |
| `018` 口袋体积分布图 | `013` | —— | `done` | `exclude` | ✗ 自己声明的例外 |
| `020` 只按体积筛口袋 | `018` | —— | `dead` | —— | ✗ `dead` 默认不进 |
| `023` 亲和力预测 | `020` | `020` | `done` | —— | ✓ **它就是那个成果** |
| `034` 导出 ONNX 交给合作者 | `023` | —— | `done` | `include` | ✓ 人手拉进来的 |

023 那一行完整的写法是 `input: 020 | pairs.csv`；`018` 和 `034` 上那两行完整的写法在
下一节。算出来的定稿流程是：

```
001 → 013 → 023 → 034        （013 → 023 这条边路过了 020 和 018，两步都被剔掉了）
```

顺序 `001` `013` `023` `034`，四步。逐步跟一遍：023 是成果 → 它写了 `input: 020`，
所以只沿 input 上溯到 020 → 020 没写 `input:`，退回 parent 018 → 018 同理退回 013
→ 013 退回 001 → 001 是根。闭包 = {023, 020, 018, 013, 001}。剔掉 `dead` 的 020 和
声明了 `exclude` 的 018，剩下三步；034 因为一行 `include` 单独进来，连带把它的
上游 023 也拉进闭包（023 本来就在）。

**每一步凭什么在流程里**也是算出来的，四选一：它就是声明的成果（`023`）／人手
`include`（`034`）／被谁的 `input:` 吃了（`013`，吃它的是 `023`）／它没写 `input:`
所以退回了 parent（`001`，是 `013` 的 parent）。这四句话在界面和 Methods 里都该
直接说出来——「这一步凭什么算进方法」是读者第一个会问的问题。

### 16.3 例外写在**那一步自己**身上：`pipeline:`

算出来的结论不总是对的。`parent` 是「我当时接着哪一步**想**」，不保证有字节流过来：
016 挂在 013b 底下只是因为想法承接自那个判定，数据却是从别处拿的——此时闭包会把
013b 连同它整条祖先链拖进 Methods，而其中一个字节都没参与这个结果。

两条出路，**都在记录里说清楚，不靠程序猜**：

1. **首选**：给那一步补一行真正的 `input:`（顺带把数据流图也修对了）；
2. 确实没有别的写法时，在**那一步自己**的 `note.md` 上写一行 `pipeline:`。

```markdown
---
id: 018
parent: 013
status: done
title: 口袋体积分布图
pipeline: exclude | 只是看一眼分布合不合理，没有产物进下游
---
```

```markdown
---
id: 034
parent: 023
status: done
title: 导出 ONNX 交给合作者
pipeline: include | 流程的最后一环，可没有别的步骤吃它的产物，闭包够不到它
---
```

| 取值 | 什么意思 |
|---|---|
| `include` | 闭包够不到它，但它确实是流程的一环。它的上游会跟着一起进来——一个输入不在流程里的成员，写进 Methods 就是一句断了的话 |
| `exclude` | 不算流程。**刻意不带贬义**：探索性的一步成功了、有价值、只是没进最终那条链 |

- 和 `branch:` 逐字同一个套路（竖线左边给机器、右边给人），因为它们回答的是同一类
  问题：**这一步自己**声明一件只有它说得清的事。**不写这一行**才是常态
- **理由必填**，这是它和 `branch:` 唯一的分歧：候选组在树上看得见，而 `pipeline:`
  除了改变一份导出之外不留任何痕迹，没有那半句话就分不清是想清楚的决定还是一次误点。
  走写入接口时只给取值不给理由会被当场拒绝
- 认不出的取值（`exclud` 这种笔误）报一条 `bad_pipeline` 并**当没写**——和
  `status:` / `branch:` 同一条路：报一声、退回默认、继续建树。竖线右边那句说明
  **原样留着**，不会被悄悄抹平
- 它**不会被子步骤继承**。新建一步会继承父步骤的 `path:`，但不继承 `pipeline:`
- **写在项目上的清单和写在这里的一行不是一回事。**这一行是这一步自己的声明，
  只影响它自己；它不是「成员清单」的另一种写法

### 16.4 三条白拿的判断

写论文时最想知道的三件事，全部从上面那份派生里落下来，一个字都不用另记。

**① 整条流程的可溯源等级 = 其中最弱的一步。**判据复用第 10 节的 L0–L4，
取的是每个成员**自己**的等级而不是它的整链等级（整链会把被剔掉的 `dead` /
`exclude` 祖先算进来，而流程说的就是「那些不算方法的一部分」）。一个数回答
「别人能不能照着做出来」，并**指名是哪一步拖的后腿**——补记录从那一步补起。

**② 流程里出现了 `dead` 的一步。**这是**必须**说出来的一条：你的结果依赖着一条
你自己已经判定走不通的路。它不是排版问题，两种可能里必有一种：

- 那个 `dead` 下错了（这条路其实走通了，只是当时先标了 `dead` 忘了改回来）；
- 或者这个依赖该换一步（真正该被吃的是别人的产物，`input:` 指错了）。

**两种都要动手，没有「知道了但先放着」这一档。**注意剔除本身不会让它消失：
被剔掉的步骤上游照样接了过来（`via`），所以流程看着是完整的——正因为看着完整，
这条诊断才必须**点名**是哪几步。

**③ 流程里有 L0/L1 的步。**这几步别人跑不起来（第 10 节：L0 是记录本身不全，
L1 是看得懂但代码/产物找不回来），投稿前就该补。它和 ① 是同一件事的两种说法：
① 给一个数和一个最弱的 id，③ 给完整的名单。

### 16.5 全部诊断

定稿流程的诊断**自成一份**，不混进项目的 warnings 列表——现存项目一个 `result:`
都没声明，把它们挂进全局警告栏等于每个项目每次打开都被念一遍，而那正是让人
从此不看警告的做法。

级别只有两档：第一条是提示级（`info`），其余六条是警告级（`warn`）。

| `code` | 级 | 什么时候报 | 该怎么办 |
|---|---|---|---|
| `pipeline_no_result` | 提示 | 一个 `result:` 都没声明 | 这是**邀请**不是缺陷：想要一条能给别人照着做的流程，就在 `project.md` 里写一行 |
| `pipeline_dead_step` | 警告 | 闭包里有 `status: dead` 的步骤 | 上面 ② 那两条出路二选一 |
| `pipeline_weak_step` | 警告 | 流程里有 L0/L1 的步 | 从点名的那几步补记录，不是从最新那一步补 |
| `pipeline_excluded_consumed` | 警告 | 一步写着 `exclude`，流程里却有成员吃着它的产物 | 两句话不能同时成立：要么删掉那行 `exclude`，要么消费者的 `input:` 指错了 |
| `pipeline_excluded_result` | 警告 | 一步既被声明成成果、自己又写着 `exclude` | 按「成果永远在流程里」处理，但两行字里一定有一行是旧的，删掉它 |
| `pipeline_cycle` | 警告 | 数据依赖成环，排不出先后 | 流程照样给出来（环上的按 id 序排最后），但写进 Methods 之前得先把环拆掉 |
| `dangling_result` | 警告 | `result:` 指向不存在的步骤 | id 写错了，或者那一步被删了（看 `project.md` 的「已删除」） |

另有一条走**项目的** warnings 列表：`bad_pipeline`（`pipeline:` 的取值认不出来）。
它和 `bad_branch` 同一条路：报一声、当没写、继续建树。

这七条一条都**不进 L0–L4**，和第 3 / 6 / 15 节那些诊断同一档：它们问的是
「这条流程讲清楚了没有」，不是「这个结果追不追得到」。

### 16.6 两条路径互相跳

界面上是两级切换：上面一级选**看的是哪一份东西**（开发路径 / 定稿流程，`p` 键），
下面一级才选**画法**（图 / 列表 / 数据流，`g` 键）。两级不是四选一。

- 从定稿流程跳回开发路径：「这一步当时有 3 个候选，为什么选了它」正是两条路径
  都留着的意义——那个岔路口只在开发路径上看得见（第 15 节）
- 反过来，开发路径上属于定稿流程的步骤带一个徽标，否则「我这一步算不算方法的
  一部分」只能靠人自己在脑子里跑一遍闭包

### 16.7 导出：三样，同一份派生

三样都从**同一份**算好的定稿流程长出来（不各算一遍），并且都**逐字节确定**
（P3）——同样的记录两次导出一致，所以该重新生成而不是把导出存进仓库。

| 导出 | 什么场合 |
|---|---|
| **图**（自包含 SVG） | 直接放进论文。无外部资源、无脚本，**黑白打印可读**——这一次不许只靠颜色区分（和第 15 节三种边同一条规矩） |
| **Methods 草稿**（markdown） | 按流程顺序，每一步写清做了什么、完整命令、代码位置（`commit` 或 `code: snapshot` + `manifest=`）、产物路径与校验和 |
| **独立页面** | 发给合作者。沿用现有静态导出那套，只含定稿流程 |

**Methods 草稿是初稿，不是成品。**它做的事只有一件：把记录里**已有的事实**按
Methods 的骨架排好。它**不生成论文腔的句子**——凭空生成的那种句子读起来最像成品，
而它描述的是一件没有发生过的事。发出去之前自己读一遍。

### 16.8 从哪个门写得进去、从哪个门读得出来

这两个键是**普通的 front-matter 键**：手写进 `project.md` / `note.md` 永远有效
（P1，文件就是数据库），下面这些只是省事的入口。

| 入口 | 怎么用 |
|---|---|
| CLI · 声明成果 | `trace_cli.py result -P <项目> <id> --note "这是什么成果"`（`--drop` 撤销） |
| CLI · 读流程与三样导出 | `trace_cli.py pipeline -P <项目> --methods --svg fig.svg --page share.html` |
| REST · 写 | `PUT` / `DELETE /api/p/{项目}/results/{id}`；`pipeline` 是建步骤和 `PATCH` 的普通字段 |
| REST · 读 | `GET /api/p/{项目}/pipeline`，三样导出各一条：`…/pipeline/figure.svg`、`…/pipeline/methods.md`、`…/pipeline/page.html` |
| MCP | `trace_result` / `trace_update_step(pipeline=…)` 写，`trace_pipeline` 读（`methods=true` 直接给草稿） |
| 网页 | 步骤编辑器里那个三选一（默认「不写这一行」）；顶栏「定稿流程」那一档（`p` 键） |
| Python | `trace_write.set_result` / `drop_result` 写，`trace_core.compute_pipeline` 读 |

`trace_cli.py build` 还会给每个声明了成果的项目多生成三样：`p/<项目>/pipeline.svg`、
`pipeline.md`、`pipeline.html` —— 静态导出里那三个按钮就指向它们。

**三样导出只有一份实现**（`trace_mcp` 里那三个纯函数），CLI、REST、MCP、`build`
和网页全走它。网页上那张图不是页面自己画的：服务模式下它 `fetch` 上面那条
`figure.svg`，静态模式下读 `build` 灌进页面的同一批字节。所以**屏幕上看到的
就是发出去的那一份**——各画一遍的话，你会对着一张图讨论、把另一张投出去。

---

## 17. 章节：一个项目里并列的几块

一个项目从来不只是「一棵树加几条死路」。真要写论文时，同一个项目内部本来就是**并列
的几块**：主实验、消融实验、数据准备。它们各有各的探索路径、各有各的成果，
在论文里各占一段 Methods。

森林（多个根）早就给了「独立的路径」，缺的只是给这几条线**起个名字**，好让
「消融这一块」能被当成**一个单元**来看：它有几步、从哪儿开始、别人能不能把它单独
重做一遍、它的 Methods 是哪一段。

一行就够，写在**开启那条线的那一步**上：

```
chapter: 消融实验 | 逐个拿掉模块，对着主实验的 023 比
```

竖线左边是**章节名**，右边是**这个章节**的说明——不是这一步的说明（这一步的说明是
`title` 和正文）。说明可选。沿用整份 front-matter「竖线左边给机器、右边给人」的惯例，
和 `input:` / `branch:` / `pipeline:` 逐字同一个语法。

### 17.1 项目 / 章节 / 分叉：先分清这三样

这是这一节唯一真正容易出错的地方，而且出错的方向是固定的：**人会拿章节去表达分叉**
（「两种方案各开一章」）。三样各问一句话就分得开：

| 问自己 | 回答 | 是哪一种 | 怎么写 |
|---|---|---|---|
| 它们会写进**同一篇论文**吗？ | **不会** —— 是两个研究，各问各的问题 | 两个**项目** | 各建一个项目，id 各自从 001 开始 |
| 这几块**要不要都留着、都写进论文**？ | **要** —— 主实验和消融都是结果的一部分 | 同一个项目的几个**章节** | 开启那条线的第一步写一行 `chapter:` |
| **这两条能同时成立吗？** | **不能** —— 同一个问题的两个答案，只能选一条走下去 | 一组**互斥候选**（第 15 节） | 每个候选各写一行 `branch: alternative`，定下来就把其余的标 `dead` |

**章节之间不互斥**，这是它和分叉的分界线。消融不是主实验的替代方案：两者同时成立、
都要留、都要写进论文。分叉正相反，最后只能剩一条。所以搞混的代价是实打实的：

- **拿章节去写分叉**：候选组那三个派生信号全部消失——这一组有谁、选了哪个、
  这个岔路口还没决定（第 15 节）。章节一个都不会给你，它根本没有「选中」这回事
- **拿分叉去写章节**：等主实验定下来，你会被迫把消融那一支标 `dead`（「选了主实验」）
  ——而 `dead` 的意思是「这条路走不通」，等于把一整节 Methods 判了死刑，
  半年后读记录的人会以为消融做失败了

一个章节内部当然照样可以有分叉（消融那条线上也会有「先试哪种拿掉法」的岔路口），
两套东西各管各的，互不干涉。

### 17.2 只标开启那条线的第一步，往下自动继承

**这是整个设计的支点。**一个步骤没写 `chapter:` 时，它属于**它 parent 的章节**，
一路往上找到第一个写了的为止（判据只有一份：`trace_core.resolve_chapters`）。
于是开启消融那一步声明一次，整条子树都属于消融。

```markdown
---
id: 030
parent: 023
status: done
title: 逐个拿掉模块，看每一块贡献多少
chapter: 消融实验 | 逐个拿掉模块，对着主实验的 023 比
input: 023 | pairs.csv
---

## 为什么
主结果出来了，但说不清哪一块在起作用。
```

**不要给每一步都标一遍。**那不是「更保险」，是把继承展开成 20 份会漂移的拷贝：
改一次章节名要改 20 个文件、把一支挪走它还带着一行过期的声明，而「章节说明归谁」
（17.4）会被 20 份同名声明搅浑。这正是 P1 禁止的那种中心索引，只是它长在了每一个
步骤自己身上。

- 想让某一步**脱离**，它自己声明一个新的章节名就行。没有「置空」这种写法：
  空的 `chapter:` 什么都没说（读侧看不见它），写入侧撤销时**整行都不写**
- 一路到根都没有人声明 → 这一步**未分章**。这不是错，**绝大多数项目就是这个状态**
- `moved:` 之后章节**自己跟着变**：把一条线从主实验挪进消融，改的是 `parent` 一个字，
  `chapter:` 一个字节都不用动。反过来说也要当心——移动一步会顺带把它整条子树的
  归属换掉，而磁盘上只多了一行 `moved:`，所以写入侧会把这次换章的名单报给你

#### 在脑子里算一遍

一个七步的项目，磁盘上一共只有 4 行 `chapter:`（`001` / `013` / `030`，外加另一棵树上
的 `050`），其余三步一个字都不写：

| 步骤 | `parent` | 这一步写的 `chapter:` | 它属于哪个章节 | 凭什么 |
|---|---|---|---|---|
| `001` 抓下全部 CIF 并清洗 | —— | `主实验/数据准备` | `主实验/数据准备` | 它自己声明的 |
| `013` 算口袋组成 | `001` | `主实验` | `主实验` | 它自己声明的（从上一段**脱离**） |
| `023` 亲和力预测 | `013` | —— | `主实验` | 沿 parent 继承，一个字都不用写 |
| `030` 逐个拿掉模块 | `023` | `消融实验` | `消融实验` | 它自己声明的（消融从这里开始） |
| `031` 去掉图注意力 | `030` | —— | `消融实验` | 继承 |
| `032` 去掉预训练 | `030` | —— | `消融实验` | 继承 |
| `050` 换个骨干从头重训 | —— | `消融实验` | `消融实验` | 另一棵树上的第二个入口（17.3） |

`030` 完整的写法还带一行 `input: 023 | pairs.csv`（它吃的就是主实验的产物）。
于是这个项目的三个章节是：`主实验/数据准备` 1 步、`主实验` 2 步、`消融实验` 4 步，
**未分章 0 步**；`消融实验` 有 **2 个入口**（`030` 和 `050`）。

### 17.3 一个章节可以横跨多棵树

消融可能是好几条独立的根——`050` 那种「换个骨干从头重训」没有任何 parent 可挂。
所以**章节是一组步骤，不必是一棵子树**，也正因为如此它不能用「子树 = 章节」来实现。

每个章节因此有一份**入口清单**（`roots`）：parent 不在本章的成员，含真正的树根。
上面那个例子里 `消融实验` 的入口是 `030` 和 `050` 两个。入口就是「这一章从哪儿开始
读」，也是跨章节那条边落地的地方（17.6）。

### 17.4 章节说明归谁

同一个章节名可能在好几处被声明（`030` 和 `050` 都写了 `chapter: 消融实验`），
而说明只该写一遍。规则：**按 id 序最早的那个带说明的声明生效**。

注意不是「最早的那个声明」：在第二处开同一个章节时不重复说明是正常写法，
按「最早的声明」算会把唯一那句说明扔掉，而那不是任何人的本意。

多处给了**不同**的说明 → 报一条 `chapter_note_conflict`，点名是哪几步、哪一句生效
（17.8）。程序不替人合并那几句话——它们都是人写的字，一个字都不动地留在各自的文件里。

### 17.5 两件**刻意不做**的事

写在这里免得后来人以为是漏了去补：

**① id 不按章节重编号。**消融不从 `001` 重新开始。`id` 是**分配顺序**不是章节内序号，
而 `[[007]]` 和论文脚注里的引用要在**整个项目里唯一**——那是只追加的地基（第 2 节：
「写下就不再改、不重发」）。按章重编号意味着同一个项目里会有两个 `001`，
于是每一处交叉引用都得先说清「哪一章的 001」，而移动一步就会把号码洗一遍。

**② 章节不嵌套。**名字里可以写 `主实验/数据准备`，界面按 `/` 分组显示
（分隔符的唯一来源是 `trace_core.CHAPTER_SEP`），但**语义上仍然是一层**：
`主实验/数据准备` 和 `主实验` 是两个平级的章节，前者不是后者的子章节，
成员也不会算进后者。真正的树形章节要么变成第二棵树（和步骤树打架），
要么逼出一份父子关系表（又是中心索引，P1 禁止）。

### 17.6 白拿的三样

章节一旦有了名字，下面三样一个字都不用另记。

**① 每个章节各有自己的定稿流程。**`result:`（第 16 节）指的是某一步，**那一步的章节
就决定了这条流程属于哪个章节**——主实验一段 Methods、消融一段，论文里本来就是两段。
接着 17.2 那个例子，`project.md` 里两行成果：

```
result: 023 | 亲和力预测：AUC 0.91（论文图 3）
result: 031 | 图 4 的消融：去掉图注意力掉 0.04
```

算出来是两组：

| 章节 | 这一章的成果 | 这一组的成员（按全局顺序） | 其中**借来的**（`external`） |
|---|---|---|---|
| `主实验` | `023` | `001` `013` `023` | `001`（借自 `主实验/数据准备`） |
| `消融实验` | `031` | `001` `013` `023` `030` `031` | `001` `013` `023`（借自另外两章） |

两组**相交**是正常的（消融当然吃着主实验的产物），所以这里是把**同一张 DAG 切开**，
不是每章各算一遍闭包——同一步在总图和分章图里位置一致（每组的顺序是全局顺序的
**子序列**）。各算一遍就会出现「屏幕上讨论的图和投出去的图不是一张」。

借来的那几步**必须留在流程里**——一个输入不在流程里的成员写进 Methods 就是一句
断了的话——但导出时该标成「借自 <哪一章>」，那正是「消融是对着主结果测的」这句话
在纸上的样子。也别拿它们去要求本章补记录：它们有自己那一章去负责。

**② 每个章节各有自己的可溯源等级**：本章成员里**最弱的那一步**说了算，并点名是谁。
它单独回答「消融这一部分别人能不能重做」——比一个项目级的总分有用得多，
因为要补的记录从来不是均匀分布的（主实验往往写得最全，消融最容易只留一个数）。
判据复用第 10 节的 L0–L4，取的是成员**自己**的等级而不是整链等级（整链会把别的
章节的祖先算进来，而那些步骤有它们自己那一章的等级去负责）。

**③ 跨章节的边要标出来，不是藏起来。**消融当然要吃主实验的产物，那条边说的正是
**「消融是对着主结果测的」**——它是两个章节之间唯一的连接，值得画出来。
两种依据各说一件事：

| 依据 | 这条边说的是 | 例子 |
|---|---|---|
| `input` | **字节**从另一章来：这一步读的是那一章的产物 | `030` 读 `023` 的 `pairs.csv` —— 消融是对着主结果测的 |
| `parent` | 这条线是从另一章的哪一步**分出去的** | `030` 挂在 `023` 底下 —— 消融从主实验的这一点开始 |

一头未分章也算跨（主实验没起名字、只有消融起了名字是常见写法，此时那条边正是消融
唯一的连接）；两头都未分章才不算。17.2 那个例子里的三条跨章节边：

```
001 → 013   parent   主实验/数据准备 → 主实验
023 → 030   parent   主实验 → 消融实验
023 → 030   input    主实验 → 消融实验（pairs.csv）
```

同一对步骤既跨 `parent` 又跨 `input` 时两条都给，它们说的不是一件事。

### 17.7 导出：按章节各出一份

第 16.7 节那三样（自包含 SVG 图 · Methods 草稿 · 独立页面）都可以**按章节导**，
判据就是 17.6 ①那份切好的分组：`主实验` 一份、`消融实验` 一份，论文里本来就是两段。

- 分章导出的产物**只能是同一份派生的视图**，不许在导出那一侧再算一遍闭包，
  理由和第 16.7 节「三样导出只有一份实现」逐字一样
- 借来的成员（`external`）要标成「借自 `<哪一章>`」，别悄悄混进本章的步骤里
- 派生的文件名**不许拿章节名原样去拼**：它合法地可以是 `主实验/数据准备`、
  `CON`、`..`，长度可到 60（17.9）。导出那一侧自己 slug 化、避开 Windows 保留名
  并**去重**（章节名不折叠大小写，所以 `Ablation` 和 `ablation` 会 slug 成同一个
  名字，靠它在清单里的序号消歧，绝不让后一份静默盖掉前一份）
- **未分章那一组照样导得出来**（组名是空的），否则它会从按章导出的清单里整条消失
  ——而它常常就是主实验（多数人只给消融起了名字，主线一直没起）。命令行和查询串上
  它写成 `-`（空串在那两处和「没给」长得一模一样）

### 17.8 四条诊断（一条都不影响等级）

前三条**自成一份**，和第 16.5 节那些一样不混进项目的 warnings 列表；最后一条走
项目的 warnings（它是一行写坏了的 front-matter，和 `bad_branch` / `bad_pipeline` 同一档）。

| `code` | 级 | 什么时候报 | 该怎么办 |
|---|---|---|---|
| `chapter_note_conflict` | 警告 | 同一个章节名在几处写了**不同**的说明 | 按 id 序最早的那句生效，其余几句在界面上一个字都不出现。留一句；本来就想说两个章节的话，把名字改开 |
| `chapter_no_result` | 提示 | 一个章节里一条 `result:` 都没有（**且这个项目别的章节有**） | 和 `pipeline_no_result` 同一个路子：这是**邀请**不是缺陷。想要这一段 Methods，就在 `project.md` 里给这一章的成果写一行 `result:` |
| `chapter_near_duplicate` | 警告 | 两个章节名只差**大小写或空白** | 几乎肯定是同一个章节被拆成了两半，两半各导出一段 Methods 而两边看着都像对的。统一成一种写法 |
| `bad_chapter` | 警告 | `chapter:` 只写了竖线右边的说明、没写名字 | 这一行不算声明（这一步仍然继承 parent 的章节），而人以为自己开了一条新线。补上名字：`chapter: 消融实验 \| 逐个拿掉模块` |

写坏的那一行**原样留在文件里**，之后每一次保存都原样写回去（`render_note` 那一支
和 `_hydrate` 是一对）：那半句话是人写的字，改一个不相干的标题就把它删掉，等于
悄悄毁掉一条记录，而那条催人补上名字的警告也会跟着一起消失。撤销声明是另一回事
（名字和说明一起清空），那时整行不写——绝不留一行空的 `chapter:`。

`chapter_no_result` 只在**这个项目已经声明过成果**时才说：一个 `result:` 都没有的
项目，该说的话 `pipeline_no_result` 已经说过一遍了，在这里按章节再说 N 遍，
就是同一件事念 N+1 次——那正是让人从此不看诊断的做法。

**「一个章节只有一个步骤 → 也许是笔误」这条刻意没做。**一个章节被开启的那一刻
必然只有一步（就是声明它的那一步），这条会在每一次**正确使用**时当场响一声；
而合法的单步章节也确实存在（一步就说完的数据准备）。会在正确用法上响的诊断，
人学会的只是忽略整个诊断栏。真正想逮的那件事（`消融实验` 被写成两个）交给
`chapter_near_duplicate`——它的判据是机械的，不靠相似度去猜。

### 17.9 章节名的规矩

章节靠**同名**成立（没有章节注册表，P1 不许有），所以「看着一样、比起来不一样」的
差别会把一章静悄悄劈成两半，而人在界面上看不出来。写入时因此抹平三种差别、
拒绝三种写法：

| 抹平 | 为什么 |
|---|---|
| 首尾空白 | `主实验 ` 和 `主实验` 是同一章。从输入框、从别处复制粘贴最常带出来的就是它 |
| 内部连续空白（含 tab / NBSP） | `Main  Experiment` 和 `Main Experiment` 同理 |
| Unicode 归一到 NFC | 组合符和预组合字在屏幕上逐像素相同，比起来却不同 |

| 拒绝 | 为什么 |
|---|---|
| 竖线 | 那一行是「名字加竖线加说明」的形状，塞进名字里会让写进去的和读回来的不是一回事 |
| 控制字符 | 看不见，却参与相等比较 |
| 超过 60 个字符 | 章节名是个**标签**（要出现在分组标题、按章导出的产物名、节点卡片上）。超长的多半是把说明写进了名字，挪到竖线右边去 |

**不折叠大小写**：中文没有大小写，而英文名里的大小写是人有意写的（`RNA 口袋` 折成
`rna 口袋` 就成了另一句话）。于是 `Ablation` 和 `ablation` 是两个章节——那正是
`chapter_near_duplicate` 要点名的东西，交给读侧提醒，比写入侧替人猜哪个才是本意好。

**也不按路径段限制字符**：`主实验/数据准备` 是合法的名字（17.5 ②）。
路径安全那一套落在导出那一侧（17.7），不靠阉割一个人写的自由文本来换。

### 17.10 一个 `chapter:` 都没有的项目，完全无感

现存的每一个项目都是这个状态，所以这一节的功能**不能让它们多出任何东西**：
不多一条警告、不多一个字段值、既有行为一个字不变。具体地：

- 森林里**没有** `chapters` 这个键（不是空的，是整个键不出现）
- 每个步骤上**没有** `chapter` 这个键
- 布局（顺序 / 轨道 / 树形 / 候选组 / 汇回）一个数都不变，warnings 一条都不多

和第 16.1 节「一个 `result:` 都没声明是常态」是同一条要求：多一个键，前端就要多一条
判空；多一条「你还没分章」的提示，每个项目每次打开都被念一遍，而那正是让人
从此不看提示的做法。

### 17.11 从哪个门写得进去、从哪个门读得出来

`chapter` 是**普通的 front-matter 键**：手写进 `note.md` 永远有效（P1，文件就是数据库），
下面这些只是省事的入口。

| 入口 | 怎么用 |
|---|---|
| 手写 | 在开启那条线的那一步的 `note.md` 里加一行。整条子树立刻跟着走 |
| CLI · 读 | `trace_cli.py chapter -P <项目>`（列章节：几步、状态分布、等级与最弱一步、成果、跨章节的边、诊断） |
| CLI · 按章看流程 / 导出 | `trace_cli.py pipeline -P <项目> --chapter 消融实验`（配 `--methods` / `--svg` / `--page`；未分章那一组写 `--chapter -`） |
| CLI · 建步骤时就给 | `trace_cli.py new -P <项目> --chapter "消融实验 \| 逐个拿掉模块"` |
| REST · 读 | `GET /api/p/{项目}/chapters`；按章节读流程与三样导出是同一批路径加 `?chapter=<章节名>`（未分章那一组是 `?chapter=-`） |
| REST · 写 | 建步骤和 `PATCH …/steps/{id}` 的普通字段：`{"chapter": "消融实验 \| 逐个拿掉模块"}`。空串 = 撤销声明 |
| MCP | `trace_read(chapters=true)` 读清单，`trace_pipeline(chapter=…)` 读某一章的流程，`trace_new_step(chapter=…)` / `trace_update_step(chapter=…)` 写 |
| 网页 | 步骤编辑器里那一栏（留空 = 跟着 parent 继承），顶栏那个章节筛选器**只在项目真有章节时才渲染** |
| Python · 写 | `trace_write.create_step(..., chapter=…)` / `update_step(patch={"chapter": …})` |
| Python · 读 | `trace_core.resolve_chapters`（每一步属于哪章）、`compute_chapters`（章节清单、入口、等级、跨章节的边、诊断）、`compute_pipeline(..., chapter_of=…)`（每章各自的定稿流程） |

三条硬约定（写入侧当场生效，手写时自己守住）：

- **空串 = 撤销声明**，回到沿 parent 继承，文件里不留一行空的 `chapter:`
- **只写说明不写名字直接拒绝**，不悄悄当成撤销——那句说明是人写的字
- **说明可选**（这是它和 `pipeline:` 唯一的分歧）：`pipeline:` 逼着写理由，因为它
  除了改变一份导出之外不留任何痕迹；而一个章节是**看得见**的（整条子树归了它、
  导出里多出一节），名字本身已经说清了它是什么

「一步属于哪个章节」只有一份判据（`trace_core.resolve_chapters`）。
任何地方都不要拿「它自己写没写 `chapter:`」当归属判断——那是**声明在哪**，
和**属于哪一章**是两件事，混用会让继承来的整条子树集体看着像未分章。

---

## 18. 给 agent 的检查清单

写完一步，逐条对一遍：

- [ ] 「为什么」写了假设，不只是动作
- [ ] 「做了什么」里的命令完整到别人能照着跑
- [ ] `done` / `dead` 写了结论；`dead` 写清了放弃理由
- [ ] 指标用表格，不是散落在句子里
- [ ] 每张图都有图注，且图注写的是结论
- [ ] 代码位置记了：`commit`，或者代码不在 git 里时记一条 `code:`（第 7 节）
- [ ] 产物位置用 `path:` 记了，顺手标上 role（`input` / `output` / …）
- [ ] 输入不是只来自 parent 那一步的话，写了 `input:`（第 6 节）
- [ ] 跑挂了也建了步骤并标 `dead`，没有默默丢掉
- [ ] 这一步和兄弟**只能选一条走下去**的话，各写了一行 `branch: alternative`，
      并在**父节点**上写了 `decision:`（第 15 节）。问自己一句：**这两条能同时成立吗？**
      能 → 就是两条普通延伸，什么都别写

写完一步再多想一句：

- [ ] 这一步有没有产生**项目级**的教训（跨若干步才成立的结论、会反复咬人的坑）？
      有就用 `trace_insight` 往 `project.md` 记一条（第 11 节）
- [ ] 真去查过 / 跑过别人的步骤，就补一条 `repro:`（第 9 节）——`failed` 也要写
- [ ] 发现某一步挂错了父节点，用移动 + 写清原因（第 14 节），
      **不要**去对调两个节点的正文
- [ ] 一组候选里终于有一条走通了，就把其余的标 `dead` 并写清放弃理由（第 15 节）——
      「选了哪个」就是这么表达的，没有别的字段可写
- [ ] 这一步**开启了并列的一块**（消融、数据准备、另一组要单独写进论文的实验）的话，
      **只在这一步**写一行 `chapter:`（第 17 节）。问自己一句：**这几块要不要都留着、
      都写进论文？**要 → 是章节；只能留一条 → 那是分叉，写 `branch: alternative`
- [ ] 底下那些步骤**一个 `chapter:` 都不要写**——它们沿 parent 继承。
      每一步各标一遍等于把继承展开成会过期的拷贝，改一次章节名要改二十个文件

要总结这个项目、要写 Methods、要把方法给别人照着做时（第 16 节）：

- [ ] **看的是定稿流程，不是整棵树。**开发路径里有 `dead`、有还没决定的岔路口，
      照着它复现或总结，等于把作者已经放弃的路当成方法写出去
- [ ] 定稿流程是空的（一个 `result:` 都没声明）就**问用户哪一步是成果**，
      不要自己挑一步——那一行是全项目唯一推导不出来的事实
- [ ] 报**整条流程**的等级，并点名最弱的那一步。单步等级答不了「别人能不能重做出来」
- [ ] 流程里出现 `dead` 的一步，**必须点名说出来**：要么那个 `dead` 下错了，
      要么这个依赖该换一步，没有「知道了但先放着」这一档
- [ ] 想改流程成员时，先补 `input:`，实在不行才在**那一步自己**身上写一行
      `pipeline:` 并**写清理由**。永远不要去项目上列一份成员清单
- [ ] 项目分了章节（第 17 节）就**按章节报**：主实验一段 Methods、消融一段，
      等级也各报各的。「主实验能重做、消融不行」比一个项目级的总分有用得多
- [ ] 分章导出时，**借来的上游**（`external`，比如消融吃着主实验的 023）要标明
      「借自哪一章」——那条边说的正是「消融是对着主结果测的」

补翻译时（第 13 节）：

- [ ] 走 `trace_translate`，没有手工新建 `note.en.md`
- [ ] 译文的 front-matter 里**只有 `title`**（项目笔记只有 `name`），
      `id` / `parent` / `status` / `commit` 一个都没有
- [ ] 小节标题逐字照抄对照表（`## Why` / `## What` / `## Result` / `## Conclusion` / `## Next`）
- [ ] 译文里的图**也**写了图注——图注是逐份文件判的，中文版写了不算数
